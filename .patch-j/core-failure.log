
> boardsignal-v10@0.1.0 test:core
> tsc -p tsconfig.tests.json && node --test .test-dist/tests/*.test.js

TAP version 13
# Subtest: valid Founding Beta Access verifies only the approved stable Chess.com player
ok 1 - valid Founding Beta Access verifies only the approved stable Chess.com player
  ---
  duration_ms: 41.273251
  type: 'test'
  ...
# Subtest: Founding Beta Access and future Chess.com OAuth share the exact stable Firebase uid
ok 2 - Founding Beta Access and future Chess.com OAuth share the exact stable Firebase uid
  ---
  duration_ms: 0.406499
  type: 'test'
  ...
# Subtest: wrong Founding Beta code is rejected
ok 3 - wrong Founding Beta code is rejected
  ---
  duration_ms: 38.113356
  type: 'test'
  ...
# Subtest: revoked Founding Beta code is rejected even when its hash is correct
ok 4 - revoked Founding Beta code is rejected even when its hash is correct
  ---
  duration_ms: 0.230378
  type: 'test'
  ...
# Subtest: repeated failures produce a temporary lockout that rejects the correct code
ok 5 - repeated failures produce a temporary lockout that rejects the correct code
  ---
  duration_ms: 186.2879
  type: 'test'
  ...
# Subtest: raw Founding Beta code is never part of the persisted credential record
ok 6 - raw Founding Beta code is never part of the persisted credential record
  ---
  duration_ms: 1.052626
  type: 'test'
  ...
# Subtest: player A access cannot authenticate against player B record
ok 7 - player A access cannot authenticate against player B record
  ---
  duration_ms: 37.984795
  type: 'test'
  ...
# Subtest: returning Firebase sessions bypass the access form and open My Player Room
ok 8 - returning Firebase sessions bypass the access form and open My Player Room
  ---
  duration_ms: 0.469407
  type: 'test'
  ...
# Subtest: public username to LIVE Desk remains available without authentication
ok 9 - public username to LIVE Desk remains available without authentication
  ---
  duration_ms: 1.55225
  type: 'test'
  ...
# Subtest: private Player Room remains owner-only and Beta Access records remain server-only
ok 10 - private Player Room remains owner-only and Beta Access records remain server-only
  ---
  duration_ms: 0.637329
  type: 'test'
  ...
# Subtest: Founder Beta Access management API remains behind shared Founder session/Basic fallback middleware without double-auth
ok 11 - Founder Beta Access management API remains behind shared Founder session/Basic fallback middleware without double-auth
  ---
  duration_ms: 0.410095
  type: 'test'
  ...
# Subtest: Founder authorization accepts native session and Basic auth while rejecting missing or invalid credentials
ok 12 - Founder authorization accepts native session and Basic auth while rejecting missing or invalid credentials
  ---
  duration_ms: 17.330515
  type: 'test'
  ...
# Subtest: Beta Access adds no Google, email-password, magic-link, URL-code, or credential logging flow
ok 13 - Beta Access adds no Google, email-password, magic-link, URL-code, or credential logging flow
  ---
  duration_ms: 0.338735
  type: 'test'
  ...
# Subtest: 1. new beta request keeps contact data out of public documents
ok 14 - 1. new beta request keeps contact data out of public documents
  ---
  duration_ms: 1.26178
  type: 'test'
  ...
# Subtest: 2. founder can approve and reject pending requests
ok 15 - 2. founder can approve and reject pending requests
  ---
  duration_ms: 0.232911
  type: 'test'
  ...
# Subtest: 3. approval reuses stable identity instead of creating a duplicate player
ok 16 - 3. approval reuses stable identity instead of creating a duplicate player
  ---
  duration_ms: 0.358535
  type: 'test'
  ...
# Subtest: 4. first member Desk is published into the persistent owner account
ok 17 - 4. first member Desk is published into the persistent owner account
  ---
  duration_ms: 1.158354
  type: 'test'
  ...
# Subtest: 5. required Universe participation is disclosed and cannot be falsely toggled off
ok 18 - 5. required Universe participation is disclosed and cannot be falsely toggled off
  ---
  duration_ms: 0.256742
  type: 'test'
  ...
# Subtest: 6. private Signals never enter public coverage
ok 19 - 6. private Signals never enter public coverage
  ---
  duration_ms: 0.242163
  type: 'test'
  ...
# Subtest: 7. BoardSignal event categories default ON
ok 20 - 7. BoardSignal event categories default ON
  ---
  duration_ms: 0.156572
  type: 'test'
  ...
# Subtest: 8. browser push permission is requested only from the explicit enable action
ok 21 - 8. browser push permission is requested only from the explicit enable action
  ---
  duration_ms: 0.173988
  type: 'test'
  ...
# Subtest: 9. in-app single-player delivery writes only to that user inbox
ok 22 - 9. in-app single-player delivery writes only to that user inbox
  ---
  duration_ms: 0.27541
  type: 'test'
  ...
# Subtest: 10. selected-player delivery resolves only selected stable user ids
ok 23 - 10. selected-player delivery resolves only selected stable user ids
  ---
  duration_ms: 0.383957
  type: 'test'
  ...
# Subtest: 11. all-active-beta delivery is restricted to active Founding Beta player accounts
ok 24 - 11. all-active-beta delivery is restricted to active Founding Beta player accounts
  ---
  duration_ms: 0.193532
  type: 'test'
  ...
# Subtest: 12. Player A cannot read Player B inbox or messages
ok 25 - 12. Player A cannot read Player B inbox or messages
  ---
  duration_ms: 0.229648
  type: 'test'
  ...
# Subtest: 13. player replies are authenticated server writes performed as themselves
ok 26 - 13. player replies are authenticated server writes performed as themselves
  ---
  duration_ms: 0.110123
  type: 'test'
  ...
# Subtest: 14. founder reads and replies to the correct private player thread
ok 27 - 14. founder reads and replies to the correct private player thread
  ---
  duration_ms: 0.171644
  type: 'test'
  ...
# Subtest: 15. push-disabled configuration keeps the in-app product usable
ok 28 - 15. push-disabled configuration keeps the in-app product usable
  ---
  duration_ms: 0.172432
  type: 'test'
  ...
# Subtest: 16. duplicate automated events are suppressed
ok 29 - 16. duplicate automated events are suppressed
  ---
  duration_ms: 0.188491
  type: 'test'
  ...
# Subtest: 17. ordinary episode engagement is capped and push respects 24 hours
ok 30 - 17. ordinary episode engagement is capped and push respects 24 hours
  ---
  duration_ms: 0.150481
  type: 'test'
  ...
# Subtest: 18. Desk Ready is deduped per Desk and can bypass the ordinary episode cap once
ok 31 - 18. Desk Ready is deduped per Desk and can bypass the ordinary episode cap once
  ---
  duration_ms: 0.115581
  type: 'test'
  ...
# Subtest: 19. forming-episode communications stay factual and do not mutate a completed Desk
ok 32 - 19. forming-episode communications stay factual and do not mutate a completed Desk
  ---
  duration_ms: 0.337431
  type: 'test'
  ...
# Subtest: 20. public search remains sports-safe and does not expose private diagnostics
ok 33 - 20. public search remains sports-safe and does not expose private diagnostics
  ---
  duration_ms: 0.167667
  type: 'test'
  ...
# Subtest: 21. returning Firebase session bypasses the Beta Access form
ok 34 - 21. returning Firebase session bypasses the Beta Access form
  ---
  duration_ms: 0.233032
  type: 'test'
  ...
# Subtest: 22. exact latest-four Desk retention remains unchanged
ok 35 - 22. exact latest-four Desk retention remains unchanged
  ---
  duration_ms: 7.315997
  type: 'test'
  ...
# Subtest: 23. deterministic Universe recognition remains the existing implementation
ok 36 - 23. deterministic Universe recognition remains the existing implementation
  ---
  duration_ms: 0.859456
  type: 'test'
  ...
# Subtest: 24. Stockfish 18 assets and depth-11 smoke target remain unchanged
ok 37 - 24. Stockfish 18 assets and depth-11 smoke target remain unchanged
  ---
  duration_ms: 0.137315
  type: 'test'
  ...
# Subtest: 25. requested screens retain readable contrast, visible focus and non-duplicated owner CTA
ok 38 - 25. requested screens retain readable contrast, visible focus and non-duplicated owner CTA
  ---
  duration_ms: 1.740148
  type: 'test'
  ...
# Subtest: anchored cadence advances in exact seven-day blocks
ok 39 - anchored cadence advances in exact seven-day blocks
  ---
  duration_ms: 1.00599
  type: 'test'
  ...
# Subtest: playable resignation requires an engine evaluation, not a termination count
ok 40 - playable resignation requires an engine evaluation, not a termination count
  ---
  duration_ms: 0.471212
  type: 'test'
  ...
# Subtest: two engine-supported queen misses select a queen-safety Red and Blue
ok 41 - two engine-supported queen misses select a queen-safety Red and Blue
  ---
  duration_ms: 2.179157
  type: 'test'
  ...
# Subtest: partial PDF-style placeholders fail publication
ok 42 - partial PDF-style placeholders fail publication
  ---
  duration_ms: 0.238316
  type: 'test'
  ...
# Subtest: one reviewed mistake is evidence, not a behavioural Red diagnosis
ok 43 - one reviewed mistake is evidence, not a behavioural Red diagnosis
  ---
  duration_ms: 0.487098
  type: 'test'
  ...
# Subtest: engine failure preserves candidates and reports the engine stage truthfully
ok 44 - engine failure preserves candidates and reports the engine stage truthfully
  ---
  duration_ms: 0.288546
  type: 'test'
  ...
# Subtest: Snoopy-style evidence selects passed-pawn Green, opponent-band Amber, and forcing-reply Red
ok 45 - Snoopy-style evidence selects passed-pawn Green, opponent-band Amber, and forcing-reply Red
  ---
  duration_ms: 0.790499
  type: 'test'
  ...
# Subtest: Chess.com OAuth stays safely disabled until real provider and server configuration exists
ok 46 - Chess.com OAuth stays safely disabled until real provider and server configuration exists
  ---
  duration_ms: 1.091894
  type: 'test'
  ...
# Subtest: Chess.com callback stays on the canonical route and accepts only registered origin variants
ok 47 - Chess.com callback stays on the canonical route and accepts only registered origin variants
  ---
  duration_ms: 0.368501
  type: 'test'
  ...
# Subtest: stable Chess.com player ID always maps to the same Firebase uid and founding entitlement
ok 48 - stable Chess.com player ID always maps to the same Firebase uid and founding entitlement
  ---
  duration_ms: 0.451822
  type: 'test'
  ...
# Subtest: existing public-username LIVE generation remains wired while OAuth is pending
ok 49 - existing public-username LIVE generation remains wired while OAuth is pending
  ---
  duration_ms: 0.167237
  type: 'test'
  ...
# Subtest: Firestore rules make private users, Desks and evidence owner-only
ok 50 - Firestore rules make private users, Desks and evidence owner-only
  ---
  duration_ms: 0.29244
  type: 'test'
  ...
# Subtest: public coverage helper withholds safely when disabled and contains no private Signal Board content when included
ok 51 - public coverage helper withholds safely when disabled and contains no private Signal Board content when included
  ---
  duration_ms: 0.491823
  type: 'test'
  ...
# Subtest: notification preparation preserves deterministic hooks while account delivery preferences default safely
ok 52 - notification preparation preserves deterministic hooks while account delivery preferences default safely
  ---
  duration_ms: 0.871326
  type: 'test'
  ...
# Subtest: Desk 5 retains only the latest four and identifies Desk 1 for heavy deletion
ok 53 - Desk 5 retains only the latest four and identifies Desk 1 for heavy deletion
  ---
  duration_ms: 7.29323
  type: 'test'
  ...
# Subtest: cross-Desk progress never mixes rating pools
ok 54 - cross-Desk progress never mixes rating pools
  ---
  duration_ms: 0.619315
  type: 'test'
  ...
# Subtest: recurrence requires repeated stable family keys and never claims a pattern was fixed
ok 55 - recurrence requires repeated stable family keys and never claims a pattern was fixed
  ---
  duration_ms: 0.715113
  type: 'test'
  ...
# Subtest: forming episode is separate factual state and cannot mutate the completed Desk
ok 56 - forming episode is separate factual state and cannot mutate the completed Desk
  ---
  duration_ms: 0.441607
  type: 'test'
  ...
# Subtest: previous Blue carries only when supported and summary families are stable evidence keys
ok 57 - previous Blue carries only when supported and summary families are stable evidence keys
  ---
  duration_ms: 0.581915
  type: 'test'
  ...
# Subtest: Stockfish 18 worker assets remain byte-for-byte unchanged
ok 58 - Stockfish 18 worker assets remain byte-for-byte unchanged
  ---
  duration_ms: 8.186909
  type: 'test'
  ...
# Subtest: Patch H fixtures A-F are deterministic and bounded
ok 59 - Patch H fixtures A-F are deterministic and bounded
  ---
  duration_ms: 0.902296
  type: 'test'
  ...
# Subtest: 1-5 new player: latest activity establishes four-slot cadence and chronology
ok 60 - 1-5 new player: latest activity establishes four-slot cadence and chronology
  ---
  duration_ms: 8.185385
  type: 'test'
  ...
# Subtest: 6-10 existing player: cadence and existing evidence are immutable inputs
ok 61 - 6-10 existing player: cadence and existing evidence are immutable inputs
  ---
  duration_ms: 0.825975
  type: 'test'
  ...
# Subtest: 11-13 sparse activity: empty slots are evaluated but never fabricated
ok 62 - 11-13 sparse activity: empty slots are evaluated but never fabricated
  ---
  duration_ms: 0.297324
  type: 'test'
  ...
# Subtest: 14-17 idempotence: existing/evaluated slots and leases prevent duplicate work
ok 63 - 14-17 idempotence: existing/evaluated slots and leases prevent duplicate work
  ---
  duration_ms: 0.337821
  type: 'test'
  ...
# Subtest: 18-23 retention: four imported Reviews equal one new-player activation, never four returns
ok 64 - 18-23 retention: four imported Reviews equal one new-player activation, never four returns
  ---
  duration_ms: 0.630232
  type: 'test'
  ...
# Subtest: 24-32 Universe: historical persistence is silent and current boards choose latest period
ok 65 - 24-32 Universe: historical persistence is silent and current boards choose latest period
  ---
  duration_ms: 1.634512
  type: 'test'
  ...
# Subtest: 33-37 Progress/Ask: imported Reviews stay in chronological private history
ok 66 - 33-37 Progress/Ask: imported Reviews stay in chronological private history
  ---
  duration_ms: 0.354014
  type: 'test'
  ...
# Subtest: 38-41 failure/API: backfill is retryable and Chess.com archive access is cached/serial
ok 67 - 38-41 failure/API: backfill is retryable and Chess.com archive access is cached/serial
  ---
  duration_ms: 0.715916
  type: 'test'
  ...
# Subtest: 42-52 regression surfaces remain wired
ok 68 - 42-52 regression surfaces remain wired
  ---
  duration_ms: 2.911579
  type: 'test'
  ...
# Subtest: G.3 Player Room integration stays presentation-safe
ok 69 - G.3 Player Room integration stays presentation-safe
  ---
  duration_ms: 0.493093
  type: 'test'
  ...
# Subtest: legacy lifecycle defaults remain backwards compatible
ok 70 - legacy lifecycle defaults remain backwards compatible
  ---
  duration_ms: 0.115856
  type: 'test'
  ...
# Subtest: H.1 1 stale June forming becomes review-check truth
ok 71 - H.1 1 stale June forming becomes review-check truth
  ---
  duration_ms: 2.291093
  type: 'test'
  ...
# Subtest: H.1 2 stale forming cannot inflate Reviews Forming metric
not ok 72 - H.1 2 stale forming cannot inflate Reviews Forming metric
  ---
  duration_ms: 2.030195
  type: 'test'
  location: '/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:54:25'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /reviewsForming:\s*rows\.filter\(\(row\) => row\.forming\)\.length/. Input:
    
    'import "server-only";\n' +
      '\n' +
      'import { FieldValue } from "firebase-admin/firestore";\n' +
      'import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";\n' +
      'import {\n' +
      '  deriveFounderOperation,\n' +
      '  summarizeFounderHistoryVisibility,\n' +
      '} from "../founderOperationsLogic";\n' +
      'import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";\n' +
      'import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";\n' +
      'import type { CompletedReviewHistoryItem } from "../reviewHistory";\n' +
      'import { getAdminDb } from "../../../utils/firebaseAdmin";\n' +
      'import { loadRecentReportPeriodTruth } from "./reviewPeriods";\n' +
      'import {\n' +
      '  FOUNDER_MATERIALIZED_VERSION,\n' +
      '  founderPendingSummaryFromInput,\n' +
      '  founderSummaryFromRow,\n' +
      '  loadFounderOperationRows,\n' +
      '  loadFounderOperationsAggregate,\n' +
      '  rebuildFounderAggregateFromSummaries,\n' +
      '  upsertFounderPendingRequestSummary,\n' +
      '  upsertFounderPlayerSummary,\n' +
      '  type FounderOperationsRow,\n' +
      '} from "./founderMaterialized";\n' +
      '\n' +
      'const MAX_ACTIVE_REVIEWS = 4;\n' +
      'const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;\n' +
      '\n' +
      'type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };\n' +
      'type OperationsAccount = BoardSignalAccount & {\n' +
      '  originalBetaPlayer?: boolean;\n' +
      '  originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;\n' +
      '  reviewHistoryBackfill?: ReviewHistoryBackfillState;\n' +
      '};\n' +
      'type StoredReview = {\n' +
      '  deskKey?: string;\n' +
      '  periodEnd?: string;\n' +
      '  publishedAt?: string;\n' +
      '  importedAt?: string;\n' +
      '  reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";\n' +
      '  countsTowardRetention?: boolean;\n' +
      '  summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };\n' +
      '  desk?: { source?: string; provenance?: { verified?: boolean } };\n' +
      '  originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };\n' +
      '};\n' +
      'type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };\n' +
      'type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };\n' +
      '\n' +
      'function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }\n' +
      'function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }\n' +
      'function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }\n' +
      'function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }\n' +
      'function historicalPublishedPeriods(account: OperationsAccount) {\n' +
      '  return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;\n' +
      '}\n' +
      'function validPeriod(data: StoredReview): ReviewPeriod | undefined {\n' +
      '  const history = data.originalBeta?.history;\n' +
      '  const start = history?.periodStart ?? data.summary?.periodStart;\n' +
      '  const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;\n' +
      '  if (!start || !end) return undefined;\n' +
      '  const lifecycle = storedReviewLifecycle(data);\n' +
      '  const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;\n' +
      '  return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;\n' +
      '}\n' +
      '\n' +
      'function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {\n' +
      '  if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };\n' +
      '  return undefined;\n' +
      '}\n' +
      '\n' +
      'async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {\n' +
      '  const db = getAdminDb();\n' +
      '  const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();\n' +
      '  const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));\n' +
      '  const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });\n' +
      '  const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));\n' +
      '  const latestNonHistorical = documents.flatMap(({ data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    return period && period.source !== "historical" ? [{ data, period }] : [];\n' +
      '  })[0];\n' +
      '  const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });\n' +
      '  const liveHistory = documents.flatMap(({ id, data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    if (!period || period.source === "historical" || data.originalBeta?.history) return [];\n' +
      '    return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];\n' +
      '  });\n' +
      '  const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));\n' +
      '\n' +
      '  const [unread, coverage, reportTruth, exceptions] = await Promise.all([\n' +
      '    db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),\n' +
      '    db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),\n' +
      '    loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),\n' +
      '    db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),\n' +
      '  ]);\n' +
      '  const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);\n' +
      '  const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;\n' +
      '  const unreadReplies = Number((unread as { size?: number }).size ?? 0);\n' +
      '  const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";\n' +
      '  const derived = deriveFounderOperation({\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    forming: cadenceWeekForming(account),\n' +
      '    latestReview,\n' +
      '    unreadReplies,\n' +
      '    unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    founderOps: account.founderOps,\n' +
      '  }, now);\n' +
      '  const officialChessState = latestOfficialChessStatePeriod(periods);\n' +
      '  const row: FounderOperationsRow = {\n' +
      '    ...derived,\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    playerId: account.chessCom.playerId,\n' +
      '    profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,\n' +
      '    avatar: account.chessCom.avatar,\n' +
      '    reviewCount: nonHistorical.length,\n' +
      '    reviewPeriods: nonHistorical,\n' +
      '    history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),\n' +
      '    latestReview,\n' +
      '    latestReportPeriod: reportTruth.periods.at(-1),\n' +
      '    recentReportPeriods: reportTruth.periods,\n' +
      '    historyCoverage: reportTruth.coverage,\n' +
      '    officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    unreadReplies,\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    pendingRequest: false,\n' +
      '    forming: derived.forming,\n' +
      '    preferredContactMethod: account.preferredContactMethod,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    accountStatus: account.accessStatus,\n' +
      '    accessStatus: account.accessStatus,\n' +
      '    identityStatus: account.identityStatus,\n' +
      '    identityReviewStatus: account.identityReviewStatus,\n' +
      '    publicHighlightsStatus,\n' +
      '    lastContactedAt: account.founderOps?.lastContactedAt,\n' +
      '    lastContactMethod: account.founderOps?.lastContactMethod,\n' +
      '    followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,\n' +
      '  };\n' +
      '  const originalReviews = Math.max(\n' +
      '    nonHistorical.filter((period) => period.source === "original").length,\n' +
      '    Object.keys(account.originalBetaHistoryPeriods ?? {}).length,\n' +
      '    account.originalBetaPlayer ? 1 : 0,\n' +
      '  );\n' +
      '  const liveReviews = Math.max(\n' +
      '    account.reviewProduction?.organicLiveReviews ?? 0,\n' +
      '    nonHistorical.fi'... 6028 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import "server-only";
    
    import { FieldValue } from "firebase-admin/firestore";
    import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";
    import {
      deriveFounderOperation,
      summarizeFounderHistoryVisibility,
    } from "../founderOperationsLogic";
    import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";
    import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";
    import type { CompletedReviewHistoryItem } from "../reviewHistory";
    import { getAdminDb } from "../../../utils/firebaseAdmin";
    import { loadRecentReportPeriodTruth } from "./reviewPeriods";
    import {
      FOUNDER_MATERIALIZED_VERSION,
      founderPendingSummaryFromInput,
      founderSummaryFromRow,
      loadFounderOperationRows,
      loadFounderOperationsAggregate,
      rebuildFounderAggregateFromSummaries,
      upsertFounderPendingRequestSummary,
      upsertFounderPlayerSummary,
      type FounderOperationsRow,
    } from "./founderMaterialized";
    
    const MAX_ACTIVE_REVIEWS = 4;
    const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;
    
    type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };
    type OperationsAccount = BoardSignalAccount & {
      originalBetaPlayer?: boolean;
      originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;
      reviewHistoryBackfill?: ReviewHistoryBackfillState;
    };
    type StoredReview = {
      deskKey?: string;
      periodEnd?: string;
      publishedAt?: string;
      importedAt?: string;
      reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";
      countsTowardRetention?: boolean;
      summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };
      desk?: { source?: string; provenance?: { verified?: boolean } };
      originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };
    };
    type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };
    type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };
    
    function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }
    function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }
    function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }
    function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }
    function historicalPublishedPeriods(account: OperationsAccount) {
      return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;
    }
    function validPeriod(data: StoredReview): ReviewPeriod | undefined {
      const history = data.originalBeta?.history;
      const start = history?.periodStart ?? data.summary?.periodStart;
      const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;
      if (!start || !end) return undefined;
      const lifecycle = storedReviewLifecycle(data);
      const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;
      return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;
    }
    
    function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {
      if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };
      return undefined;
    }
    
    async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {
      const db = getAdminDb();
      const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();
      const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));
      const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });
      const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));
      const latestNonHistorical = documents.flatMap(({ data }) => {
        const period = validPeriod(data);
        return period && period.source !== "historical" ? [{ data, period }] : [];
      })[0];
      const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });
      const liveHistory = documents.flatMap(({ id, data }) => {
        const period = validPeriod(data);
        if (!period || period.source === "historical" || data.originalBeta?.history) return [];
        return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];
      });
      const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));
    
      const [unread, coverage, reportTruth, exceptions] = await Promise.all([
        db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),
        db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),
        loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),
        db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),
      ]);
      const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);
      const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;
      const unreadReplies = Number((unread as { size?: number }).size ?? 0);
      const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";
      const derived = deriveFounderOperation({
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        preferredContactValue: account.preferredContactValue,
        lastSeenAt: account.lastSeenAt,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        forming: cadenceWeekForming(account),
        latestReview,
        unreadReplies,
        unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionCount: activeExceptions.length,
        identityConflict: identityConflict(account),
        founderOps: account.founderOps,
      }, now);
      const officialChessState = latestOfficialChessStatePeriod(periods);
      const row: FounderOperationsRow = {
        ...derived,
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        playerId: account.chessCom.playerId,
        profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,
        avatar: account.chessCom.avatar,
        reviewCount: nonHistorical.length,
        reviewPeriods: nonHistorical,
        history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),
        latestReview,
        latestReportPeriod: reportTruth.periods.at(-1),
        recentReportPeriods: reportTruth.periods,
        historyCoverage: reportTruth.coverage,
        officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        lastSeenAt: account.lastSeenAt,
        unreadReplies,
        exceptionCount: activeExceptions.length,
        exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),
        identityConflict: identityConflict(account),
        pendingRequest: false,
        forming: derived.forming,
        preferredContactMethod: account.preferredContactMethod,
        preferredContactValue: account.preferredContactValue,
        accountStatus: account.accessStatus,
        accessStatus: account.accessStatus,
        identityStatus: account.identityStatus,
        identityReviewStatus: account.identityReviewStatus,
        publicHighlightsStatus,
        lastContactedAt: account.founderOps?.lastContactedAt,
        lastContactMethod: account.founderOps?.lastContactMethod,
        followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,
      };
      const originalReviews = Math.max(
        nonHistorical.filter((period) => period.source === "original").length,
        Object.keys(account.originalBetaHistoryPeriods ?? {}).length,
        account.originalBetaPlayer ? 1 : 0,
      );
      const liveReviews = Math.max(
        account.reviewProduction?.organicLiveReviews ?? 0,
        nonHistorical.filter((period) => period.source === "live").length,
      );
      const historicalPeriods = Math.max(
        account.reviewProduction?.historicalBackfillReviews ?? 0,
        historicalPublishedPeriods(account),
      );
      return founderSummaryFromRow({
        playerId: account.chessCom.playerId,
        uid: account.uid,
        row,
        originalReviews,
        liveReviews,
        historicalPeriods,
        reviewProduction: account.reviewProduction,
        activationBaseline: account.reviewHistoryBackfill?.activationBaseline === true,
        updatedAt: now.toISOString(),
      });
    }
    
    export async function refreshFounderPlayerSummary(account: OperationsAccount, now = new Date()) {
      if (account.role !== "player") return;
      const summary = await sourcePlayerSummary(account, now);
      await upsertFounderPlayerSummary(summary, now);
      return summary;
    }
    
    export async function refreshFounderPlayerSummaryByUid(uid: string, now = new Date()) {
      const snapshot = await getAdminDb().collection("users").doc(uid).get();
      if (!snapshot.exists) return;
      return refreshFounderPlayerSummary(snapshot.data() as OperationsAccount, now);
    }
    
    function bootstrapLeaseRef() { return getAdminDb().collection("founderOperationsState").doc("bootstrapLease"); }
    async function acquireBootstrapLease(now = new Date()) {
      const db = getAdminDb();
      return db.runTransaction(async (transaction) => {
        const [current, lease] = await Promise.all([transaction.get(db.collection("founderOperationsState").doc("current")), transaction.get(bootstrapLeaseRef())]);
        if (current.exists && current.data()?.version === FOUNDER_MATERIALIZED_VERSION && current.data()?.bootstrapComplete === true) return false;
        const data = lease.data() as { leaseUntil?: string } | undefined;
        if (data?.leaseUntil && data.leaseUntil > now.toISOString()) return false;
        transaction.set(bootstrapLeaseRef(), { leaseUntil: new Date(now.getTime() + FOUNDER_BOOTSTRAP_LEASE_MS).toISOString(), startedAt: now.toISOString() }, { merge: false });
        return true;
      });
    }
    
    async function bootstrapFounderMaterialization(now = new Date()) {
      if (!await acquireBootstrapLease(now)) return undefined;
      try {
        const db = getAdminDb();
        const [users, requests] = await Promise.all([db.collection("users").get(), db.collection("betaRequests").get()]);
        const accounts = users.docs.map((document) => document.data() as OperationsAccount).filter((account) => account.role === "player");
        for (const account of accounts) await refreshFounderPlayerSummary(account, now);
        for (const document of requests.docs) {
          const request = { id: document.id, ...document.data() } as PendingRequest;
          if (request.status === "pending") await upsertFounderPendingRequestSummary(founderPendingSummaryFromInput(request, now), now);
        }
        const aggregate = await rebuildFounderAggregateFromSummaries(now);
        await bootstrapLeaseRef().delete().catch(() => undefined);
        return aggregate;
      } catch (error) {
        await bootstrapLeaseRef().delete().catch(() => undefined);
        throw error;
      }
    }
    
    export async function founderOperationsSnapshot(now = new Date(), includeRows = false) {
      let aggregate = await loadFounderOperationsAggregate(now);
      if (!aggregate) {
        aggregate = await bootstrapFounderMaterialization(now);
        if (!aggregate) throw Object.assign(new Error("Founder operations are being prepared. Try again shortly."), { status: 503, code: "FOUNDER_OPERATIONS_BOOTSTRAPPING", retryAfterSeconds: 5 });
      }
      return { ...aggregate, rows: includeRows ? await loadFounderOperationRows() : [] };
    }
    
    async function accountForFounderOps(uidInput: unknown) {
      const uid = String(uidInput ?? "").trim();
      if (!uid || uid.startsWith("request:") || uid.includes("/")) throw Object.assign(new Error("A valid BoardSignal player is required."), { status: 400 });
      const ref = getAdminDb().collection("users").doc(uid);
      const snapshot = await ref.get();
      if (!snapshot.exists) throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      const account = snapshot.data() as OperationsAccount;
      if (account.role !== "player") throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      return { ref, account };
    }
    
    export async function markFounderContacted(uidInput: unknown, methodInput: unknown, now = new Date()) {
      const method = String(methodInput ?? "") as BoardSignalFounderContactMethod;
      if (!["email", "discord", "telegram", "chesscom", "other"].includes(method)) throw Object.assign(new Error("Choose a valid contact method."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), lastContactedAt: now.toISOString(), lastContactMethod: method };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function snoozeFounderFollowUp(uidInput: unknown, daysInput: unknown, now = new Date()) {
      const days = Number(daysInput);
      if (![1, 3, 7].includes(days)) throw Object.assign(new Error("Choose a 1, 3, or 7 day snooze."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), followUpSnoozedUntil: new Date(now.getTime() + days * 24 * 60 * 60 * 1000).toISOString() };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function clearFounderFollowUpSnooze(uidInput: unknown, now = new Date()) {
      const { ref, account } = await accountForFounderOps(uidInput);
      await ref.update({ "founderOps.followUpSnoozedUntil": FieldValue.delete() });
      const founderOps = { ...(account.founderOps ?? {}) };
      delete founderOps.followUpSnoozedUntil;
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return { cleared: true };
    }
    
    export function normalizeFounderUsername(value?: string) { return normalize(value); }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:57:22)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: H.1 3 stale forming cannot pass Reviews Forming filter
ok 73 - H.1 3 stale forming cannot pass Reviews Forming filter
  ---
  duration_ms: 0.393349
  type: 'test'
  ...
# Subtest: H.1 4 future forming remains current
ok 74 - H.1 4 future forming remains current
  ---
  duration_ms: 0.213744
  type: 'test'
  ...
# Subtest: H.1 5 due grace remains intact
ok 75 - H.1 5 due grace remains intact
  ---
  duration_ms: 0.239031
  type: 'test'
  ...
# Subtest: H.1 fixture D stored non-forming overdue still requires a Review check
ok 76 - H.1 fixture D stored non-forming overdue still requires a Review check
  ---
  duration_ms: 0.285567
  type: 'test'
  ...
# Subtest: H.1 6 ready-not-seen priority remains intact
ok 77 - H.1 6 ready-not-seen priority remains intact
  ---
  duration_ms: 0.223923
  type: 'test'
  ...
# Subtest: H.1 7 past review date is not labelled NEXT REVIEW
ok 78 - H.1 7 past review date is not labelled NEXT REVIEW
  ---
  duration_ms: 0.547504
  type: 'test'
  ...
# Subtest: H.1 8 historical backfill remains separate from qualifying Review retention
not ok 79 - H.1 8 historical backfill remains separate from qualifying Review retention
  ---
  duration_ms: 1.719491
  type: 'test'
  location: '/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:106:25'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /verifiedDocuments = allVerifiedDocuments\.filter\(\(\{ period \}\) => period\.source !== "historical"\)/. Input:
    
    'import "server-only";\n' +
      '\n' +
      'import { FieldValue } from "firebase-admin/firestore";\n' +
      'import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";\n' +
      'import {\n' +
      '  deriveFounderOperation,\n' +
      '  summarizeFounderHistoryVisibility,\n' +
      '} from "../founderOperationsLogic";\n' +
      'import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";\n' +
      'import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";\n' +
      'import type { CompletedReviewHistoryItem } from "../reviewHistory";\n' +
      'import { getAdminDb } from "../../../utils/firebaseAdmin";\n' +
      'import { loadRecentReportPeriodTruth } from "./reviewPeriods";\n' +
      'import {\n' +
      '  FOUNDER_MATERIALIZED_VERSION,\n' +
      '  founderPendingSummaryFromInput,\n' +
      '  founderSummaryFromRow,\n' +
      '  loadFounderOperationRows,\n' +
      '  loadFounderOperationsAggregate,\n' +
      '  rebuildFounderAggregateFromSummaries,\n' +
      '  upsertFounderPendingRequestSummary,\n' +
      '  upsertFounderPlayerSummary,\n' +
      '  type FounderOperationsRow,\n' +
      '} from "./founderMaterialized";\n' +
      '\n' +
      'const MAX_ACTIVE_REVIEWS = 4;\n' +
      'const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;\n' +
      '\n' +
      'type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };\n' +
      'type OperationsAccount = BoardSignalAccount & {\n' +
      '  originalBetaPlayer?: boolean;\n' +
      '  originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;\n' +
      '  reviewHistoryBackfill?: ReviewHistoryBackfillState;\n' +
      '};\n' +
      'type StoredReview = {\n' +
      '  deskKey?: string;\n' +
      '  periodEnd?: string;\n' +
      '  publishedAt?: string;\n' +
      '  importedAt?: string;\n' +
      '  reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";\n' +
      '  countsTowardRetention?: boolean;\n' +
      '  summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };\n' +
      '  desk?: { source?: string; provenance?: { verified?: boolean } };\n' +
      '  originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };\n' +
      '};\n' +
      'type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };\n' +
      'type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };\n' +
      '\n' +
      'function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }\n' +
      'function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }\n' +
      'function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }\n' +
      'function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }\n' +
      'function historicalPublishedPeriods(account: OperationsAccount) {\n' +
      '  return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;\n' +
      '}\n' +
      'function validPeriod(data: StoredReview): ReviewPeriod | undefined {\n' +
      '  const history = data.originalBeta?.history;\n' +
      '  const start = history?.periodStart ?? data.summary?.periodStart;\n' +
      '  const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;\n' +
      '  if (!start || !end) return undefined;\n' +
      '  const lifecycle = storedReviewLifecycle(data);\n' +
      '  const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;\n' +
      '  return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;\n' +
      '}\n' +
      '\n' +
      'function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {\n' +
      '  if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };\n' +
      '  return undefined;\n' +
      '}\n' +
      '\n' +
      'async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {\n' +
      '  const db = getAdminDb();\n' +
      '  const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();\n' +
      '  const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));\n' +
      '  const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });\n' +
      '  const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));\n' +
      '  const latestNonHistorical = documents.flatMap(({ data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    return period && period.source !== "historical" ? [{ data, period }] : [];\n' +
      '  })[0];\n' +
      '  const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });\n' +
      '  const liveHistory = documents.flatMap(({ id, data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    if (!period || period.source === "historical" || data.originalBeta?.history) return [];\n' +
      '    return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];\n' +
      '  });\n' +
      '  const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));\n' +
      '\n' +
      '  const [unread, coverage, reportTruth, exceptions] = await Promise.all([\n' +
      '    db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),\n' +
      '    db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),\n' +
      '    loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),\n' +
      '    db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),\n' +
      '  ]);\n' +
      '  const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);\n' +
      '  const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;\n' +
      '  const unreadReplies = Number((unread as { size?: number }).size ?? 0);\n' +
      '  const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";\n' +
      '  const derived = deriveFounderOperation({\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    forming: cadenceWeekForming(account),\n' +
      '    latestReview,\n' +
      '    unreadReplies,\n' +
      '    unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    founderOps: account.founderOps,\n' +
      '  }, now);\n' +
      '  const officialChessState = latestOfficialChessStatePeriod(periods);\n' +
      '  const row: FounderOperationsRow = {\n' +
      '    ...derived,\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    playerId: account.chessCom.playerId,\n' +
      '    profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,\n' +
      '    avatar: account.chessCom.avatar,\n' +
      '    reviewCount: nonHistorical.length,\n' +
      '    reviewPeriods: nonHistorical,\n' +
      '    history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),\n' +
      '    latestReview,\n' +
      '    latestReportPeriod: reportTruth.periods.at(-1),\n' +
      '    recentReportPeriods: reportTruth.periods,\n' +
      '    historyCoverage: reportTruth.coverage,\n' +
      '    officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    unreadReplies,\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    pendingRequest: false,\n' +
      '    forming: derived.forming,\n' +
      '    preferredContactMethod: account.preferredContactMethod,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    accountStatus: account.accessStatus,\n' +
      '    accessStatus: account.accessStatus,\n' +
      '    identityStatus: account.identityStatus,\n' +
      '    identityReviewStatus: account.identityReviewStatus,\n' +
      '    publicHighlightsStatus,\n' +
      '    lastContactedAt: account.founderOps?.lastContactedAt,\n' +
      '    lastContactMethod: account.founderOps?.lastContactMethod,\n' +
      '    followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,\n' +
      '  };\n' +
      '  const originalReviews = Math.max(\n' +
      '    nonHistorical.filter((period) => period.source === "original").length,\n' +
      '    Object.keys(account.originalBetaHistoryPeriods ?? {}).length,\n' +
      '    account.originalBetaPlayer ? 1 : 0,\n' +
      '  );\n' +
      '  const liveReviews = Math.max(\n' +
      '    account.reviewProduction?.organicLiveReviews ?? 0,\n' +
      '    nonHistorical.fi'... 6028 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import "server-only";
    
    import { FieldValue } from "firebase-admin/firestore";
    import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";
    import {
      deriveFounderOperation,
      summarizeFounderHistoryVisibility,
    } from "../founderOperationsLogic";
    import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";
    import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";
    import type { CompletedReviewHistoryItem } from "../reviewHistory";
    import { getAdminDb } from "../../../utils/firebaseAdmin";
    import { loadRecentReportPeriodTruth } from "./reviewPeriods";
    import {
      FOUNDER_MATERIALIZED_VERSION,
      founderPendingSummaryFromInput,
      founderSummaryFromRow,
      loadFounderOperationRows,
      loadFounderOperationsAggregate,
      rebuildFounderAggregateFromSummaries,
      upsertFounderPendingRequestSummary,
      upsertFounderPlayerSummary,
      type FounderOperationsRow,
    } from "./founderMaterialized";
    
    const MAX_ACTIVE_REVIEWS = 4;
    const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;
    
    type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };
    type OperationsAccount = BoardSignalAccount & {
      originalBetaPlayer?: boolean;
      originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;
      reviewHistoryBackfill?: ReviewHistoryBackfillState;
    };
    type StoredReview = {
      deskKey?: string;
      periodEnd?: string;
      publishedAt?: string;
      importedAt?: string;
      reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";
      countsTowardRetention?: boolean;
      summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };
      desk?: { source?: string; provenance?: { verified?: boolean } };
      originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };
    };
    type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };
    type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };
    
    function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }
    function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }
    function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }
    function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }
    function historicalPublishedPeriods(account: OperationsAccount) {
      return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;
    }
    function validPeriod(data: StoredReview): ReviewPeriod | undefined {
      const history = data.originalBeta?.history;
      const start = history?.periodStart ?? data.summary?.periodStart;
      const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;
      if (!start || !end) return undefined;
      const lifecycle = storedReviewLifecycle(data);
      const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;
      return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;
    }
    
    function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {
      if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };
      return undefined;
    }
    
    async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {
      const db = getAdminDb();
      const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();
      const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));
      const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });
      const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));
      const latestNonHistorical = documents.flatMap(({ data }) => {
        const period = validPeriod(data);
        return period && period.source !== "historical" ? [{ data, period }] : [];
      })[0];
      const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });
      const liveHistory = documents.flatMap(({ id, data }) => {
        const period = validPeriod(data);
        if (!period || period.source === "historical" || data.originalBeta?.history) return [];
        return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];
      });
      const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));
    
      const [unread, coverage, reportTruth, exceptions] = await Promise.all([
        db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),
        db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),
        loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),
        db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),
      ]);
      const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);
      const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;
      const unreadReplies = Number((unread as { size?: number }).size ?? 0);
      const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";
      const derived = deriveFounderOperation({
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        preferredContactValue: account.preferredContactValue,
        lastSeenAt: account.lastSeenAt,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        forming: cadenceWeekForming(account),
        latestReview,
        unreadReplies,
        unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionCount: activeExceptions.length,
        identityConflict: identityConflict(account),
        founderOps: account.founderOps,
      }, now);
      const officialChessState = latestOfficialChessStatePeriod(periods);
      const row: FounderOperationsRow = {
        ...derived,
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        playerId: account.chessCom.playerId,
        profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,
        avatar: account.chessCom.avatar,
        reviewCount: nonHistorical.length,
        reviewPeriods: nonHistorical,
        history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),
        latestReview,
        latestReportPeriod: reportTruth.periods.at(-1),
        recentReportPeriods: reportTruth.periods,
        historyCoverage: reportTruth.coverage,
        officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        lastSeenAt: account.lastSeenAt,
        unreadReplies,
        exceptionCount: activeExceptions.length,
        exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),
        identityConflict: identityConflict(account),
        pendingRequest: false,
        forming: derived.forming,
        preferredContactMethod: account.preferredContactMethod,
        preferredContactValue: account.preferredContactValue,
        accountStatus: account.accessStatus,
        accessStatus: account.accessStatus,
        identityStatus: account.identityStatus,
        identityReviewStatus: account.identityReviewStatus,
        publicHighlightsStatus,
        lastContactedAt: account.founderOps?.lastContactedAt,
        lastContactMethod: account.founderOps?.lastContactMethod,
        followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,
      };
      const originalReviews = Math.max(
        nonHistorical.filter((period) => period.source === "original").length,
        Object.keys(account.originalBetaHistoryPeriods ?? {}).length,
        account.originalBetaPlayer ? 1 : 0,
      );
      const liveReviews = Math.max(
        account.reviewProduction?.organicLiveReviews ?? 0,
        nonHistorical.filter((period) => period.source === "live").length,
      );
      const historicalPeriods = Math.max(
        account.reviewProduction?.historicalBackfillReviews ?? 0,
        historicalPublishedPeriods(account),
      );
      return founderSummaryFromRow({
        playerId: account.chessCom.playerId,
        uid: account.uid,
        row,
        originalReviews,
        liveReviews,
        historicalPeriods,
        reviewProduction: account.reviewProduction,
        activationBaseline: account.reviewHistoryBackfill?.activationBaseline === true,
        updatedAt: now.toISOString(),
      });
    }
    
    export async function refreshFounderPlayerSummary(account: OperationsAccount, now = new Date()) {
      if (account.role !== "player") return;
      const summary = await sourcePlayerSummary(account, now);
      await upsertFounderPlayerSummary(summary, now);
      return summary;
    }
    
    export async function refreshFounderPlayerSummaryByUid(uid: string, now = new Date()) {
      const snapshot = await getAdminDb().collection("users").doc(uid).get();
      if (!snapshot.exists) return;
      return refreshFounderPlayerSummary(snapshot.data() as OperationsAccount, now);
    }
    
    function bootstrapLeaseRef() { return getAdminDb().collection("founderOperationsState").doc("bootstrapLease"); }
    async function acquireBootstrapLease(now = new Date()) {
      const db = getAdminDb();
      return db.runTransaction(async (transaction) => {
        const [current, lease] = await Promise.all([transaction.get(db.collection("founderOperationsState").doc("current")), transaction.get(bootstrapLeaseRef())]);
        if (current.exists && current.data()?.version === FOUNDER_MATERIALIZED_VERSION && current.data()?.bootstrapComplete === true) return false;
        const data = lease.data() as { leaseUntil?: string } | undefined;
        if (data?.leaseUntil && data.leaseUntil > now.toISOString()) return false;
        transaction.set(bootstrapLeaseRef(), { leaseUntil: new Date(now.getTime() + FOUNDER_BOOTSTRAP_LEASE_MS).toISOString(), startedAt: now.toISOString() }, { merge: false });
        return true;
      });
    }
    
    async function bootstrapFounderMaterialization(now = new Date()) {
      if (!await acquireBootstrapLease(now)) return undefined;
      try {
        const db = getAdminDb();
        const [users, requests] = await Promise.all([db.collection("users").get(), db.collection("betaRequests").get()]);
        const accounts = users.docs.map((document) => document.data() as OperationsAccount).filter((account) => account.role === "player");
        for (const account of accounts) await refreshFounderPlayerSummary(account, now);
        for (const document of requests.docs) {
          const request = { id: document.id, ...document.data() } as PendingRequest;
          if (request.status === "pending") await upsertFounderPendingRequestSummary(founderPendingSummaryFromInput(request, now), now);
        }
        const aggregate = await rebuildFounderAggregateFromSummaries(now);
        await bootstrapLeaseRef().delete().catch(() => undefined);
        return aggregate;
      } catch (error) {
        await bootstrapLeaseRef().delete().catch(() => undefined);
        throw error;
      }
    }
    
    export async function founderOperationsSnapshot(now = new Date(), includeRows = false) {
      let aggregate = await loadFounderOperationsAggregate(now);
      if (!aggregate) {
        aggregate = await bootstrapFounderMaterialization(now);
        if (!aggregate) throw Object.assign(new Error("Founder operations are being prepared. Try again shortly."), { status: 503, code: "FOUNDER_OPERATIONS_BOOTSTRAPPING", retryAfterSeconds: 5 });
      }
      return { ...aggregate, rows: includeRows ? await loadFounderOperationRows() : [] };
    }
    
    async function accountForFounderOps(uidInput: unknown) {
      const uid = String(uidInput ?? "").trim();
      if (!uid || uid.startsWith("request:") || uid.includes("/")) throw Object.assign(new Error("A valid BoardSignal player is required."), { status: 400 });
      const ref = getAdminDb().collection("users").doc(uid);
      const snapshot = await ref.get();
      if (!snapshot.exists) throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      const account = snapshot.data() as OperationsAccount;
      if (account.role !== "player") throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      return { ref, account };
    }
    
    export async function markFounderContacted(uidInput: unknown, methodInput: unknown, now = new Date()) {
      const method = String(methodInput ?? "") as BoardSignalFounderContactMethod;
      if (!["email", "discord", "telegram", "chesscom", "other"].includes(method)) throw Object.assign(new Error("Choose a valid contact method."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), lastContactedAt: now.toISOString(), lastContactMethod: method };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function snoozeFounderFollowUp(uidInput: unknown, daysInput: unknown, now = new Date()) {
      const days = Number(daysInput);
      if (![1, 3, 7].includes(days)) throw Object.assign(new Error("Choose a 1, 3, or 7 day snooze."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), followUpSnoozedUntil: new Date(now.getTime() + days * 24 * 60 * 60 * 1000).toISOString() };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function clearFounderFollowUpSnooze(uidInput: unknown, now = new Date()) {
      const { ref, account } = await accountForFounderOps(uidInput);
      await ref.update({ "founderOps.followUpSnoozedUntil": FieldValue.delete() });
      const founderOps = { ...(account.founderOps ?? {}) };
      delete founderOps.followUpSnoozedUntil;
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return { cleared: true };
    }
    
    export function normalizeFounderUsername(value?: string) { return normalize(value); }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:108:22)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: H.1 9 no-activity is evaluated history, not a Review
ok 80 - H.1 9 no-activity is evaluated history, not a Review
  ---
  duration_ms: 0.498838
  type: 'test'
  ...
# Subtest: H.1 10 partial history reports x of 4
ok 81 - H.1 10 partial history reports x of 4
  ---
  duration_ms: 0.697312
  type: 'test'
  ...
# Subtest: H.1 11 complete history reports evaluated state truthfully
ok 82 - H.1 11 complete history reports evaluated state truthfully
  ---
  duration_ms: 0.13562
  type: 'test'
  ...
# Subtest: H.1 correction complete history cannot regress when an existing Review ages out
ok 83 - H.1 correction complete history cannot regress when an existing Review ages out
  ---
  duration_ms: 0.163243
  type: 'test'
  ...
# Subtest: H.1 correction missing historical slot is not fabricated before complete
ok 84 - H.1 correction missing historical slot is not fabricated before complete
  ---
  duration_ms: 0.146653
  type: 'test'
  ...
# Subtest: H.1 correction Founder detail labels qualifying Review truthfully
not ok 85 - H.1 correction Founder detail labels qualifying Review truthfully
  ---
  duration_ms: 0.464261
  type: 'test'
  location: '/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:189:25'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /<dt>Latest qualifying Review<\/dt><dd>\{row\.latestReview\?\.periodLabel \?\? "No qualifying Review"\}<\/dd>/. Input:
    
    '"use client";\n' +
      '\n' +
      'import Link from "next/link";\n' +
      'import { useCallback, useEffect, useMemo, useState } from "react";\n' +
      'import { ChevronLeft, ChevronRight, ExternalLink, LoaderCircle, RefreshCcw, Search } from "lucide-react";\n' +
      'import type { CanonicalReportPeriod, ReviewHistoryCoverage } from "@/lib/boardsignal/reviewPeriods";\n' +
      'import {\n' +
      '  filterFounderOperationRows,\n' +
      '  sortFounderOperationRows,\n' +
      '  type FounderHistoryVisibility,\n' +
      '  type FounderOperationComparableRow,\n' +
      '  type FounderOperationFilter,\n' +
      '  type FounderOperationSort,\n' +
      '  type FounderOpsContactMethod,\n' +
      '} from "@/lib/boardsignal/founderOperationsLogic";\n' +
      '\n' +
      'type OperationsRow = FounderOperationComparableRow & {\n' +
      '  playerId?: number; profileUrl?: string; avatar?: string; accountStatus?: string; identityStatus?: string; identityReviewStatus?: string;\n' +
      '  publicHighlightsStatus?: string; preferredContactMethod?: string; preferredContactValue?: string;\n' +
      '  latestReview?: { periodStart?: string; periodEnd?: string; periodLabel?: string; publishedAt?: string };\n' +
      '  latestReportPeriod?: CanonicalReportPeriod; recentReportPeriods?: CanonicalReportPeriod[]; historyCoverage?: ReviewHistoryCoverage;\n' +
      '  officialChessStatePeriod?: { periodStart?: string; periodEnd?: string; periodLabel?: string };\n' +
      '  reviewPeriods: Array<{ periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" }>;\n' +
      '  history: FounderHistoryVisibility; lastContactedAt?: string; lastContactMethod?: FounderOpsContactMethod; followUpSnoozedUntil?: string;\n' +
      '  pendingRequestId?: string; exceptionTitles: string[]; accessStatus?: string;\n' +
      '};\n' +
      'type OperationsResponse = {\n' +
      '  generatedAt: string;\n' +
      '  attention: { newRequests: number; followUpsDue: number; unreadReplies: number; exceptions: number; identityConflicts: number };\n' +
      '  metrics: { activePlayers: number; reviewsForming: number; reviewsReady: number; followUpsDue: number; notSeenRecently: number; unreadReplies: number };\n' +
      '  validation: { playersServed: number; verifiedReviews: number; originalReviews: number; liveReviews: number; historicalPeriods: number; totalReviewsProduced: number; originalToLive: number; r2Plus: number; r3Plus: number; r4: number; dataCompleteness: string };\n' +
      '  rows: OperationsRow[];\n' +
      '};\n' +
      '\n' +
      'const PAGE_SIZE = 20;\n' +
      'const FILTERS: Array<[FounderOperationFilter, string]> = [["all","ALL"],["attention","ATTENTION"],["new_requests","NEW REQUESTS"],["follow_up_due","FOLLOW-UP DUE"],["reviews_ready","REVIEWS READY"],["reviews_forming","REVIEWS FORMING"],["unread_replies","UNREAD REPLIES"],["not_seen","NOT SEEN 7D+"],["identity","IDENTITY"],["exceptions","EXCEPTIONS"]];\n' +
      'const SORTS: Array<[FounderOperationSort, string]> = [["attention","ATTENTION"],["next_review","REVIEW DATE"],["last_seen","LAST SEEN"],["review_count","REVIEW COUNT"],["username","USERNAME"]];\n' +
      'const CONTACT_METHODS: Array<[FounderOpsContactMethod, string]> = [["email","EMAIL"],["discord","DISCORD"],["telegram","TELEGRAM"],["chesscom","CHESS.COM"],["other","OTHER"]];\n' +
      '\n' +
      'function displayDate(value?: string, empty = "—") { if (!value) return empty; const parsed = Date.parse(value); return Number.isFinite(parsed) ? new Date(parsed).toLocaleDateString("en-GB", { day:"numeric", month:"short", year:"numeric" }) : empty; }\n' +
      'function relativeSeen(value?: string) { if (!value) return "NEVER / NOT RECORDED"; const parsed=Date.parse(value); if(!Number.isFinite(parsed)) return "NOT RECORDED"; const days=Math.max(0,Math.floor((Date.now()-parsed)/(24*60*60*1000))); return days===0?"Today":days===1?"1d ago":`${days}d ago`; }\n' +
      'function followUpLabel(row: OperationsRow) { if(row.followUpStatus==="snoozed") return `SNOOZED UNTIL ${displayDate(row.followUpSnoozedUntil)}`; if(row.followUpStatus==="due") return "DUE NOW"; if(row.followUpStatus==="upcoming") return displayDate(row.followUpDueAt); if(row.followUpStatus==="check") return "CHECK"; return "No follow-up"; }\n' +
      'function stateTone(row: OperationsRow) { if(row.exceptionCount||row.reviewCheckRequired||row.identityConflict) return "error"; if(row.unreadReplies||row.pendingRequest||row.readyNotSeen||row.followUpStatus==="due") return "processing"; return "ready"; }\n' +
      'function defaultContactMethod(row: OperationsRow): FounderOpsContactMethod { if(row.lastContactMethod) return row.lastContactMethod; if(row.preferredContactMethod==="email"||row.preferredContactMethod==="discord"||row.preferredContactMethod==="telegram") return row.preferredContactMethod; return row.profileUrl?"chesscom":"other"; }\n' +
      'function historyDetail(history: FounderHistoryVisibility) { if(!history.totalSlots) return {primary:"Not started",secondary:"Historical onboarding has not established a target window yet."}; const state=history.status==="retryable"?" · retryable":history.status==="complete"?" · complete":" · pending"; return {primary:`${history.evaluatedSlots} / ${history.totalSlots} evaluated`,secondary:`${history.reviewSlots} Review${history.reviewSlots===1?"":"s"} · ${history.noActivitySlots} no-activity${state}`}; }\n' +
      '\n' +
      'async function fetchOperations(url: string) {\n' +
      '  const response = await fetch(url, { cache: "no-store" });\n' +
      '  const body = await response.json() as { ok?: boolean; operations?: OperationsResponse; error?: string };\n' +
      '  if (!response.ok || !body.ok || !body.operations) throw new Error(body.error ?? "Founder operations could not be loaded.");\n' +
      '  return body.operations;\n' +
      '}\n' +
      '\n' +
      'export default function FounderOperationsConsole() {\n' +
      '  const [operations,setOperations]=useState<OperationsResponse|null>(null);\n' +
      '  const [rowsLoaded,setRowsLoaded]=useState(false);\n' +
      '  const [error,setError]=useState(""); const [loading,setLoading]=useState(true); const [rowsLoading,setRowsLoading]=useState(false);\n' +
      '  const [filter,setFilter]=useState<FounderOperationFilter>("attention"); const [sort,setSort]=useState<FounderOperationSort>("attention"); const [search,setSearch]=useState(""); const [page,setPage]=useState(1);\n' +
      '  const [busy,setBusy]=useState<string|null>(null); const [contactMethods,setContactMethods]=useState<Record<string,FounderOpsContactMethod>>({});\n' +
      '\n' +
      '  const loadSummary=useCallback(async()=>{ setLoading(true); setError(""); try { const next=await fetchOperations("/api/admin/boardsignal/operations"); setOperations((current)=>({...next,rows:current?.rows??[]})); } catch(reason){setError(reason instanceof Error?reason.message:"Founder operations could not be loaded.");} finally{setLoading(false);} },[]);\n' +
      '  const loadRows=useCallback(async()=>{ setRowsLoading(true); setError(""); try { const next=await fetchOperations("/api/admin/boardsignal/operations?view=rows"); setOperations(next); setRowsLoaded(true); const hasAttention=next.rows.some(row=>row.attentionReasons.length>0); setFilter(current=>current==="attention"&&!hasAttention?"all":current); } catch(reason){setError(reason instanceof Error?reason.message:"Player operations could not be loaded.");} finally{setRowsLoading(false);} },[]);\n' +
      '  useEffect(()=>{void loadSummary();},[loadSummary]); useEffect(()=>{setPage(1);},[filter,sort,search]);\n' +
      '  const filtered=useMemo(()=>operations&&rowsLoaded?sortFounderOperationRows(filterFounderOperationRows(operations.rows,filter,search),sort) as OperationsRow[]:[],[filter,operations,rowsLoaded,search,sort]);\n' +
      '  const pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE)); const shown=filtered.slice((Math.min(page,pages)-1)*PAGE_SIZE,Math.min(page,pages)*PAGE_SIZE);\n' +
      '\n' +
      '  async function mutate(row:OperationsRow,action:"markContacted"|"snooze"|"clearSnooze",extra:Record<string,unknown>={}){ if(row.uid.startsWith("request:")) return; setBusy(`${row.uid}:${action}`);setError("");try{const response=await fetch("/api/admin/boardsignal/operations",{method:"POST",headers:{"Content-Type":"application/json"},cache:"no-store",body:JSON.stringify({action,uid:row.uid,...extra})});const body=await response.json() as {ok?:boolean;error?:string};if(!response.ok||!body.ok)throw new Error(body.error??"Founder follow-up state could not be updated.");await Promise.all([loadSummary(),loadRows()]);}catch(reason){setError(reason instanceof Error?reason.message:"Founder follow-up state could not be updated.");}finally{setBusy(null);} }\n' +
      '  function chooseFilter(next:FounderOperationFilter){setFilter(next);void loadRows().then(()=>document.getElementById("founder-live-operations")?.scrollIntoView({behavior:"smooth",block:"start"}));}\n' +
      '\n' +
      '  if(error&&!operations)return <p className="form-error" role="alert">{error}</p>;\n' +
      '  if(!operations)return <div className="founder-directory-loading"><LoaderCircle className="button-spinner"/> Loading live Founder operations</div>;\n' +
      '  const attentionCards:Array<[string,number,FounderOperationFilter]>=[["New requests",operations.attention.newRequests,"new_requests"],["Follow-ups due",operations.attention.followUpsDue,"follow_up_due"],["Unread replies",operations.attention.unreadReplies,"unread_replies"],["Review / system exceptions",operations.attention.exceptions,"exceptions"],["Identity conflicts",operations.attention.identityConflicts,"identity"]];\n' +
      '  const liveMetrics=[["ACTIVE PLAYERS",operations.metrics.activePlayers],["REVIEWS FORMING",operations.metrics.reviewsForming],["REVIEWS READY",operations.metrics.reviewsReady],["FOLLOW-UPS DUE",operations.metrics.followUpsDue],["NOT SEEN RECENTLY",operations.metrics.notSeenRecently],["UNREAD REPLIES",operations.metrics.unreadReplies]];\n' +
      '  const validation=[["TOTAL REVIEWS",operations.validation.totalReviewsProduced],["ORIGINAL MANUAL",operations.validation.originalReviews],["ORGANIC LIVE",operations.validation.liveReviews],["HISTORICAL ONBOARDING",operations.validation.historicalPeriods],["RETENTION REVIEWS",operations.validation.verifiedReviews],["PLAYERS SERVED",operations.validation.playersServed],["ORIGINAL → LIVE",operations.validation.originalToLive],["R2+",operations.validation.r2Plus],["R3+",operations.validation.r3Plus],["R4+",operations.validation.r4]];\n' +
      '\n' +
      '  return <div className="founder-ops-console">\n' +
      '    <section className="founder-attention-section" aria-labelledby="attention-now-heading"><div className="founder-ops-heading"><div><p className="kicke'... 9693 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    "use client";
    
    import Link from "next/link";
    import { useCallback, useEffect, useMemo, useState } from "react";
    import { ChevronLeft, ChevronRight, ExternalLink, LoaderCircle, RefreshCcw, Search } from "lucide-react";
    import type { CanonicalReportPeriod, ReviewHistoryCoverage } from "@/lib/boardsignal/reviewPeriods";
    import {
      filterFounderOperationRows,
      sortFounderOperationRows,
      type FounderHistoryVisibility,
      type FounderOperationComparableRow,
      type FounderOperationFilter,
      type FounderOperationSort,
      type FounderOpsContactMethod,
    } from "@/lib/boardsignal/founderOperationsLogic";
    
    type OperationsRow = FounderOperationComparableRow & {
      playerId?: number; profileUrl?: string; avatar?: string; accountStatus?: string; identityStatus?: string; identityReviewStatus?: string;
      publicHighlightsStatus?: string; preferredContactMethod?: string; preferredContactValue?: string;
      latestReview?: { periodStart?: string; periodEnd?: string; periodLabel?: string; publishedAt?: string };
      latestReportPeriod?: CanonicalReportPeriod; recentReportPeriods?: CanonicalReportPeriod[]; historyCoverage?: ReviewHistoryCoverage;
      officialChessStatePeriod?: { periodStart?: string; periodEnd?: string; periodLabel?: string };
      reviewPeriods: Array<{ periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" }>;
      history: FounderHistoryVisibility; lastContactedAt?: string; lastContactMethod?: FounderOpsContactMethod; followUpSnoozedUntil?: string;
      pendingRequestId?: string; exceptionTitles: string[]; accessStatus?: string;
    };
    type OperationsResponse = {
      generatedAt: string;
      attention: { newRequests: number; followUpsDue: number; unreadReplies: number; exceptions: number; identityConflicts: number };
      metrics: { activePlayers: number; reviewsForming: number; reviewsReady: number; followUpsDue: number; notSeenRecently: number; unreadReplies: number };
      validation: { playersServed: number; verifiedReviews: number; originalReviews: number; liveReviews: number; historicalPeriods: number; totalReviewsProduced: number; originalToLive: number; r2Plus: number; r3Plus: number; r4: number; dataCompleteness: string };
      rows: OperationsRow[];
    };
    
    const PAGE_SIZE = 20;
    const FILTERS: Array<[FounderOperationFilter, string]> = [["all","ALL"],["attention","ATTENTION"],["new_requests","NEW REQUESTS"],["follow_up_due","FOLLOW-UP DUE"],["reviews_ready","REVIEWS READY"],["reviews_forming","REVIEWS FORMING"],["unread_replies","UNREAD REPLIES"],["not_seen","NOT SEEN 7D+"],["identity","IDENTITY"],["exceptions","EXCEPTIONS"]];
    const SORTS: Array<[FounderOperationSort, string]> = [["attention","ATTENTION"],["next_review","REVIEW DATE"],["last_seen","LAST SEEN"],["review_count","REVIEW COUNT"],["username","USERNAME"]];
    const CONTACT_METHODS: Array<[FounderOpsContactMethod, string]> = [["email","EMAIL"],["discord","DISCORD"],["telegram","TELEGRAM"],["chesscom","CHESS.COM"],["other","OTHER"]];
    
    function displayDate(value?: string, empty = "—") { if (!value) return empty; const parsed = Date.parse(value); return Number.isFinite(parsed) ? new Date(parsed).toLocaleDateString("en-GB", { day:"numeric", month:"short", year:"numeric" }) : empty; }
    function relativeSeen(value?: string) { if (!value) return "NEVER / NOT RECORDED"; const parsed=Date.parse(value); if(!Number.isFinite(parsed)) return "NOT RECORDED"; const days=Math.max(0,Math.floor((Date.now()-parsed)/(24*60*60*1000))); return days===0?"Today":days===1?"1d ago":`${days}d ago`; }
    function followUpLabel(row: OperationsRow) { if(row.followUpStatus==="snoozed") return `SNOOZED UNTIL ${displayDate(row.followUpSnoozedUntil)}`; if(row.followUpStatus==="due") return "DUE NOW"; if(row.followUpStatus==="upcoming") return displayDate(row.followUpDueAt); if(row.followUpStatus==="check") return "CHECK"; return "No follow-up"; }
    function stateTone(row: OperationsRow) { if(row.exceptionCount||row.reviewCheckRequired||row.identityConflict) return "error"; if(row.unreadReplies||row.pendingRequest||row.readyNotSeen||row.followUpStatus==="due") return "processing"; return "ready"; }
    function defaultContactMethod(row: OperationsRow): FounderOpsContactMethod { if(row.lastContactMethod) return row.lastContactMethod; if(row.preferredContactMethod==="email"||row.preferredContactMethod==="discord"||row.preferredContactMethod==="telegram") return row.preferredContactMethod; return row.profileUrl?"chesscom":"other"; }
    function historyDetail(history: FounderHistoryVisibility) { if(!history.totalSlots) return {primary:"Not started",secondary:"Historical onboarding has not established a target window yet."}; const state=history.status==="retryable"?" · retryable":history.status==="complete"?" · complete":" · pending"; return {primary:`${history.evaluatedSlots} / ${history.totalSlots} evaluated`,secondary:`${history.reviewSlots} Review${history.reviewSlots===1?"":"s"} · ${history.noActivitySlots} no-activity${state}`}; }
    
    async function fetchOperations(url: string) {
      const response = await fetch(url, { cache: "no-store" });
      const body = await response.json() as { ok?: boolean; operations?: OperationsResponse; error?: string };
      if (!response.ok || !body.ok || !body.operations) throw new Error(body.error ?? "Founder operations could not be loaded.");
      return body.operations;
    }
    
    export default function FounderOperationsConsole() {
      const [operations,setOperations]=useState<OperationsResponse|null>(null);
      const [rowsLoaded,setRowsLoaded]=useState(false);
      const [error,setError]=useState(""); const [loading,setLoading]=useState(true); const [rowsLoading,setRowsLoading]=useState(false);
      const [filter,setFilter]=useState<FounderOperationFilter>("attention"); const [sort,setSort]=useState<FounderOperationSort>("attention"); const [search,setSearch]=useState(""); const [page,setPage]=useState(1);
      const [busy,setBusy]=useState<string|null>(null); const [contactMethods,setContactMethods]=useState<Record<string,FounderOpsContactMethod>>({});
    
      const loadSummary=useCallback(async()=>{ setLoading(true); setError(""); try { const next=await fetchOperations("/api/admin/boardsignal/operations"); setOperations((current)=>({...next,rows:current?.rows??[]})); } catch(reason){setError(reason instanceof Error?reason.message:"Founder operations could not be loaded.");} finally{setLoading(false);} },[]);
      const loadRows=useCallback(async()=>{ setRowsLoading(true); setError(""); try { const next=await fetchOperations("/api/admin/boardsignal/operations?view=rows"); setOperations(next); setRowsLoaded(true); const hasAttention=next.rows.some(row=>row.attentionReasons.length>0); setFilter(current=>current==="attention"&&!hasAttention?"all":current); } catch(reason){setError(reason instanceof Error?reason.message:"Player operations could not be loaded.");} finally{setRowsLoading(false);} },[]);
      useEffect(()=>{void loadSummary();},[loadSummary]); useEffect(()=>{setPage(1);},[filter,sort,search]);
      const filtered=useMemo(()=>operations&&rowsLoaded?sortFounderOperationRows(filterFounderOperationRows(operations.rows,filter,search),sort) as OperationsRow[]:[],[filter,operations,rowsLoaded,search,sort]);
      const pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE)); const shown=filtered.slice((Math.min(page,pages)-1)*PAGE_SIZE,Math.min(page,pages)*PAGE_SIZE);
    
      async function mutate(row:OperationsRow,action:"markContacted"|"snooze"|"clearSnooze",extra:Record<string,unknown>={}){ if(row.uid.startsWith("request:")) return; setBusy(`${row.uid}:${action}`);setError("");try{const response=await fetch("/api/admin/boardsignal/operations",{method:"POST",headers:{"Content-Type":"application/json"},cache:"no-store",body:JSON.stringify({action,uid:row.uid,...extra})});const body=await response.json() as {ok?:boolean;error?:string};if(!response.ok||!body.ok)throw new Error(body.error??"Founder follow-up state could not be updated.");await Promise.all([loadSummary(),loadRows()]);}catch(reason){setError(reason instanceof Error?reason.message:"Founder follow-up state could not be updated.");}finally{setBusy(null);} }
      function chooseFilter(next:FounderOperationFilter){setFilter(next);void loadRows().then(()=>document.getElementById("founder-live-operations")?.scrollIntoView({behavior:"smooth",block:"start"}));}
    
      if(error&&!operations)return <p className="form-error" role="alert">{error}</p>;
      if(!operations)return <div className="founder-directory-loading"><LoaderCircle className="button-spinner"/> Loading live Founder operations</div>;
      const attentionCards:Array<[string,number,FounderOperationFilter]>=[["New requests",operations.attention.newRequests,"new_requests"],["Follow-ups due",operations.attention.followUpsDue,"follow_up_due"],["Unread replies",operations.attention.unreadReplies,"unread_replies"],["Review / system exceptions",operations.attention.exceptions,"exceptions"],["Identity conflicts",operations.attention.identityConflicts,"identity"]];
      const liveMetrics=[["ACTIVE PLAYERS",operations.metrics.activePlayers],["REVIEWS FORMING",operations.metrics.reviewsForming],["REVIEWS READY",operations.metrics.reviewsReady],["FOLLOW-UPS DUE",operations.metrics.followUpsDue],["NOT SEEN RECENTLY",operations.metrics.notSeenRecently],["UNREAD REPLIES",operations.metrics.unreadReplies]];
      const validation=[["TOTAL REVIEWS",operations.validation.totalReviewsProduced],["ORIGINAL MANUAL",operations.validation.originalReviews],["ORGANIC LIVE",operations.validation.liveReviews],["HISTORICAL ONBOARDING",operations.validation.historicalPeriods],["RETENTION REVIEWS",operations.validation.verifiedReviews],["PLAYERS SERVED",operations.validation.playersServed],["ORIGINAL → LIVE",operations.validation.originalToLive],["R2+",operations.validation.r2Plus],["R3+",operations.validation.r3Plus],["R4+",operations.validation.r4]];
    
      return <div className="founder-ops-console">
        <section className="founder-attention-section" aria-labelledby="attention-now-heading"><div className="founder-ops-heading"><div><p className="kicker">ATTENTION NOW</p><h2 id="attention-now-heading">What needs action.</h2><p>Deterministic operational state only. No player scoring or behavioural profiling.</p></div><button className="button button-quiet" type="button" onClick={()=>void loadSummary()} disabled={loading}>{loading?<LoaderCircle className="button-spinner" size={15}/>:<RefreshCcw size={15}/>} Refresh</button></div><div className="founder-attention-grid">{attentionCards.map(([label,value,target])=><button type="button" key={label} className={value?"has-attention":""} onClick={()=>chooseFilter(target)}><span>{label}</span><strong>{value}</strong><small>{value?"Open matching players":"Nothing flagged"}</small></button>)}</div></section>
        <section className="founder-live-metrics" aria-labelledby="live-ops-metrics-heading"><div className="founder-ops-heading"><div><p className="kicker">LIVE OPERATIONS</p><h2 id="live-ops-metrics-heading">Current player lifecycle.</h2><p>“Reviews forming” means an active player has an established cadence-aligned current week. “Reviews ready” means a completed Review was published after the player's last recorded visit — not proof that the Review itself was opened.</p></div></div><div className="founder-compact-metrics">{liveMetrics.map(([label,value])=><article key={String(label)}><span>{label}</span><strong>{value}</strong>{label==="REVIEWS READY"?<small>Ready · not seen since</small>:null}</article>)}</div></section>
        <section className="founder-validation-section" aria-labelledby="validation-heading"><div className="founder-ops-heading"><div><p className="kicker">VALIDATION TO DATE</p><h2 id="validation-heading">What BoardSignal can prove.</h2><p>Product evidence from durable BoardSignal Review records and legitimate Original Beta history.</p></div></div><div className="founder-validation-grid">{validation.map(([label,value])=><article key={String(label)}><span>{label}</span><strong>{value}</strong></article>)}</div><p className="founder-data-completeness">{operations.validation.dataCompleteness}</p></section>
    
        <section className="founder-live-directory" id="founder-live-operations" aria-labelledby="live-player-operations-heading"><div className="founder-ops-heading"><div><p className="kicker">LIVE PLAYER OPERATIONS</p><h2 id="live-player-operations-heading">Players and the next operational action.</h2><p>{rowsLoaded?`${filtered.length} matching row${filtered.length===1?"":"s"} · at most ${PAGE_SIZE} shown per page.`:"Individual player rows load only when you open this operational detail view."}</p></div>{!rowsLoaded?<button className="button button-dark" type="button" onClick={()=>void loadRows()} disabled={rowsLoading}>{rowsLoading?<LoaderCircle className="button-spinner" size={15}/>:null} Open player operations</button>:null}</div>
          {error?<p className="form-error" role="alert">{error}</p>:null}
          {rowsLoaded?<><div className="founder-ops-controls"><label className="founder-ops-search"><span>Search player or contact</span><div><Search size={15}/><input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Username or contact"/></div></label><label><span>Sort</span><select value={sort} onChange={event=>setSort(event.target.value as FounderOperationSort)}>{SORTS.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label></div><div className="founder-filter-strip" aria-label="Player operation filters">{FILTERS.map(([value,label])=><button type="button" key={value} className={filter===value?"active":""} aria-pressed={filter===value} onClick={()=>setFilter(value)}>{label}</button>)}</div>
          {shown.length?<div className="founder-ops-table" role="table" aria-label="Live player operations"><div className="founder-ops-table-head" role="row"><span>PLAYER</span><span>REVIEWS</span><span>HISTORY</span><span>CURRENT STATE</span><span>LAST SEEN</span><span>REVIEW DATE</span><span>FOLLOW-UP</span><span>CONTACT</span><span>ACTION</span></div>{shown.map(row=>{const history=historyDetail(row.history);return <article className="founder-ops-row" role="row" key={row.uid}><div className="founder-ops-main-row"><div data-label="PLAYER"><strong>{row.username}</strong>{row.playerId?<small>Chess.com ID {row.playerId}</small>:<small>Pending request</small>}</div><div data-label="REVIEWS"><strong>{row.reviewCount} qualifying</strong></div><div data-label="HISTORY" className="founder-history-cell"><strong>{history.primary}</strong><small>{history.secondary}</small><small><b>LATEST REPORT</b> · {row.latestReportPeriod?.outcome==="review"?"REVIEW":row.latestReportPeriod?.outcome==="no_activity"?"NO ACTIVITY":"SYNCING"} · {row.latestReportPeriod?.periodLabel??"No completed report period"}</small><small><b>OFFICIAL CHESS STATE</b> · {row.officialChessStatePeriod?.periodLabel??"No game-bearing Review"}</small></div><div data-label="CURRENT STATE"><span className={`state-pill ${stateTone(row)}`}>{row.currentState}</span></div><div data-label="LAST SEEN"><strong>{relativeSeen(row.lastSeenAt)}</strong></div><div data-label={row.reviewDateLabel}><small className="founder-review-date-label">{row.reviewDateLabel}</small><strong>{displayDate(row.nextDeskDueAt)}</strong></div><div data-label="FOLLOW-UP"><strong>{followUpLabel(row)}</strong></div><div data-label="CONTACT"><strong>{row.preferredContactMethod?row.preferredContactMethod.toUpperCase():row.profileUrl?"CHESS.COM":"—"}</strong><small>{row.preferredContactValue??(row.profileUrl?"Profile available":"Not confirmed")}</small></div><div data-label="ACTION"><Link className="button button-quiet" href={row.pendingRequestId?`/admin/players?request=${encodeURIComponent(row.pendingRequestId)}`:`/admin/players?player=${encodeURIComponent(String(row.playerId??row.username))}`}>MANAGE</Link></div></div>
          <details className="founder-ops-details"><summary>Details</summary><div className="founder-ops-detail-grid"><dl><div><dt>Stable Chess.com ID</dt><dd>{row.playerId??"Not yet recorded"}</dd></div><div><dt>Identity</dt><dd>{row.identityStatus??"Not recorded"}{row.identityReviewStatus?` · ${row.identityReviewStatus}`:""}</dd></div><div><dt>Account</dt><dd>{row.accountStatus??"Pending request"}</dd></div><div><dt>Public highlights</dt><dd>{row.publicHighlightsStatus??"Not available yet"}</dd></div><div><dt>Latest completed report period</dt><dd>{row.latestReportPeriod?`${row.latestReportPeriod.periodLabel} · ${row.latestReportPeriod.outcome==="review"?"REVIEW":row.latestReportPeriod.outcome==="no_activity"?"NO ACTIVITY":"SYNCING"}`:"None"}</dd></div><div><dt>Official chess state period</dt><dd>{row.officialChessStatePeriod?.periodLabel??"No game-bearing Review"}</dd></div><div><dt>Unread replies</dt><dd>{row.unreadReplies}</dd></div><div><dt>Follow-up reason</dt><dd>{row.attentionReasons.join(" · ")||"No operational attention"}</dd></div></dl><div className="founder-review-periods"><span>LATEST VERIFIED REVIEW PERIODS</span>{row.reviewPeriods.length?<ol>{row.reviewPeriods.map(review=><li key={`${review.periodStart}:${review.periodEnd}`}><strong>{review.periodLabel??`${review.periodStart} → ${review.periodEnd}`}</strong><small>{review.source==="original"?"Original Beta provenance":"Live digital Review"}</small></li>)}</ol>:<p>No verified completed Review periods stored.</p>}{row.exceptionTitles.length?<div className="founder-ops-exceptions"><strong>Operational exceptions</strong>{row.exceptionTitles.map(title=><p key={title}>{title}</p>)}</div>:null}</div></div><div className="founder-followup-actions"><div><label htmlFor={`contact-${row.uid}`}>Contact method</label><select id={`contact-${row.uid}`} value={contactMethods[row.uid]??defaultContactMethod(row)} disabled={row.uid.startsWith("request:")||busy!==null} onChange={event=>setContactMethods(values=>({...values,[row.uid]:event.target.value as FounderOpsContactMethod}))}>{CONTACT_METHODS.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select><button className="button button-dark" type="button" disabled={row.uid.startsWith("request:")||busy!==null} onClick={()=>void mutate(row,"markContacted",{method:contactMethods[row.uid]??defaultContactMethod(row)})}>{busy===`${row.uid}:markContacted`?<LoaderCircle className="button-spinner" size={14}/>:null} MARK CONTACTED</button><small>Records that you contacted this player. It does not send anything.</small></div><div><span>Snooze normal follow-up</span><div>{[1,3,7].map(days=><button className="button button-outline" type="button" key={days} disabled={row.uid.startsWith("request:")||busy!==null} onClick={()=>void mutate(row,"snooze",{days})}>{days} DAY{days===1?"":"S"}</button>)}{row.followUpSnoozedUntil?<button className="button button-quiet" type="button" disabled={busy!==null} onClick={()=>void mutate(row,"clearSnooze")}>CLEAR SNOOZE</button>:null}</div><small>Snooze never hides unread replies, identity conflicts or system exceptions.</small></div>{row.profileUrl?<a className="text-link" href={row.profileUrl} target="_blank" rel="noreferrer">Open Chess.com profile <ExternalLink size={14}/></a>:null}</div></details></article>})}</div>:<div className="universe-empty"><p>No players match this operational view.</p></div>}<nav className="founder-pagination" aria-label="Player operations pages"><button type="button" className="button button-quiet" disabled={page<=1} onClick={()=>setPage(value=>Math.max(1,value-1))}><ChevronLeft size={15}/> Previous</button><span>Page {Math.min(page,pages)} of {pages}</span><button type="button" className="button button-quiet" disabled={page>=pages} onClick={()=>setPage(value=>Math.min(pages,value+1))}>Next <ChevronRight size={15}/></button></nav></>:null}
        </section>
      </div>;
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:191:22)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: H.1 12 Original Beta and organic retention semantics remain intact
not ok 86 - H.1 12 Original Beta and organic retention semantics remain intact
  ---
  duration_ms: 0.49708
  type: 'test'
  location: '/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:196:25'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /source: review\.source/. Input:
    
    'import "server-only";\n' +
      '\n' +
      'import { FieldValue } from "firebase-admin/firestore";\n' +
      'import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";\n' +
      'import {\n' +
      '  deriveFounderOperation,\n' +
      '  summarizeFounderHistoryVisibility,\n' +
      '} from "../founderOperationsLogic";\n' +
      'import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";\n' +
      'import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";\n' +
      'import type { CompletedReviewHistoryItem } from "../reviewHistory";\n' +
      'import { getAdminDb } from "../../../utils/firebaseAdmin";\n' +
      'import { loadRecentReportPeriodTruth } from "./reviewPeriods";\n' +
      'import {\n' +
      '  FOUNDER_MATERIALIZED_VERSION,\n' +
      '  founderPendingSummaryFromInput,\n' +
      '  founderSummaryFromRow,\n' +
      '  loadFounderOperationRows,\n' +
      '  loadFounderOperationsAggregate,\n' +
      '  rebuildFounderAggregateFromSummaries,\n' +
      '  upsertFounderPendingRequestSummary,\n' +
      '  upsertFounderPlayerSummary,\n' +
      '  type FounderOperationsRow,\n' +
      '} from "./founderMaterialized";\n' +
      '\n' +
      'const MAX_ACTIVE_REVIEWS = 4;\n' +
      'const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;\n' +
      '\n' +
      'type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };\n' +
      'type OperationsAccount = BoardSignalAccount & {\n' +
      '  originalBetaPlayer?: boolean;\n' +
      '  originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;\n' +
      '  reviewHistoryBackfill?: ReviewHistoryBackfillState;\n' +
      '};\n' +
      'type StoredReview = {\n' +
      '  deskKey?: string;\n' +
      '  periodEnd?: string;\n' +
      '  publishedAt?: string;\n' +
      '  importedAt?: string;\n' +
      '  reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";\n' +
      '  countsTowardRetention?: boolean;\n' +
      '  summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };\n' +
      '  desk?: { source?: string; provenance?: { verified?: boolean } };\n' +
      '  originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };\n' +
      '};\n' +
      'type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };\n' +
      'type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };\n' +
      '\n' +
      'function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }\n' +
      'function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }\n' +
      'function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }\n' +
      'function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }\n' +
      'function historicalPublishedPeriods(account: OperationsAccount) {\n' +
      '  return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;\n' +
      '}\n' +
      'function validPeriod(data: StoredReview): ReviewPeriod | undefined {\n' +
      '  const history = data.originalBeta?.history;\n' +
      '  const start = history?.periodStart ?? data.summary?.periodStart;\n' +
      '  const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;\n' +
      '  if (!start || !end) return undefined;\n' +
      '  const lifecycle = storedReviewLifecycle(data);\n' +
      '  const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;\n' +
      '  return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;\n' +
      '}\n' +
      '\n' +
      'function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {\n' +
      '  if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };\n' +
      '  return undefined;\n' +
      '}\n' +
      '\n' +
      'async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {\n' +
      '  const db = getAdminDb();\n' +
      '  const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();\n' +
      '  const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));\n' +
      '  const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });\n' +
      '  const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));\n' +
      '  const latestNonHistorical = documents.flatMap(({ data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    return period && period.source !== "historical" ? [{ data, period }] : [];\n' +
      '  })[0];\n' +
      '  const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });\n' +
      '  const liveHistory = documents.flatMap(({ id, data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    if (!period || period.source === "historical" || data.originalBeta?.history) return [];\n' +
      '    return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];\n' +
      '  });\n' +
      '  const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));\n' +
      '\n' +
      '  const [unread, coverage, reportTruth, exceptions] = await Promise.all([\n' +
      '    db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),\n' +
      '    db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),\n' +
      '    loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),\n' +
      '    db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),\n' +
      '  ]);\n' +
      '  const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);\n' +
      '  const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;\n' +
      '  const unreadReplies = Number((unread as { size?: number }).size ?? 0);\n' +
      '  const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";\n' +
      '  const derived = deriveFounderOperation({\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    forming: cadenceWeekForming(account),\n' +
      '    latestReview,\n' +
      '    unreadReplies,\n' +
      '    unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    founderOps: account.founderOps,\n' +
      '  }, now);\n' +
      '  const officialChessState = latestOfficialChessStatePeriod(periods);\n' +
      '  const row: FounderOperationsRow = {\n' +
      '    ...derived,\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    playerId: account.chessCom.playerId,\n' +
      '    profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,\n' +
      '    avatar: account.chessCom.avatar,\n' +
      '    reviewCount: nonHistorical.length,\n' +
      '    reviewPeriods: nonHistorical,\n' +
      '    history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),\n' +
      '    latestReview,\n' +
      '    latestReportPeriod: reportTruth.periods.at(-1),\n' +
      '    recentReportPeriods: reportTruth.periods,\n' +
      '    historyCoverage: reportTruth.coverage,\n' +
      '    officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    unreadReplies,\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    pendingRequest: false,\n' +
      '    forming: derived.forming,\n' +
      '    preferredContactMethod: account.preferredContactMethod,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    accountStatus: account.accessStatus,\n' +
      '    accessStatus: account.accessStatus,\n' +
      '    identityStatus: account.identityStatus,\n' +
      '    identityReviewStatus: account.identityReviewStatus,\n' +
      '    publicHighlightsStatus,\n' +
      '    lastContactedAt: account.founderOps?.lastContactedAt,\n' +
      '    lastContactMethod: account.founderOps?.lastContactMethod,\n' +
      '    followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,\n' +
      '  };\n' +
      '  const originalReviews = Math.max(\n' +
      '    nonHistorical.filter((period) => period.source === "original").length,\n' +
      '    Object.keys(account.originalBetaHistoryPeriods ?? {}).length,\n' +
      '    account.originalBetaPlayer ? 1 : 0,\n' +
      '  );\n' +
      '  const liveReviews = Math.max(\n' +
      '    account.reviewProduction?.organicLiveReviews ?? 0,\n' +
      '    nonHistorical.fi'... 6028 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import "server-only";
    
    import { FieldValue } from "firebase-admin/firestore";
    import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";
    import {
      deriveFounderOperation,
      summarizeFounderHistoryVisibility,
    } from "../founderOperationsLogic";
    import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";
    import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";
    import type { CompletedReviewHistoryItem } from "../reviewHistory";
    import { getAdminDb } from "../../../utils/firebaseAdmin";
    import { loadRecentReportPeriodTruth } from "./reviewPeriods";
    import {
      FOUNDER_MATERIALIZED_VERSION,
      founderPendingSummaryFromInput,
      founderSummaryFromRow,
      loadFounderOperationRows,
      loadFounderOperationsAggregate,
      rebuildFounderAggregateFromSummaries,
      upsertFounderPendingRequestSummary,
      upsertFounderPlayerSummary,
      type FounderOperationsRow,
    } from "./founderMaterialized";
    
    const MAX_ACTIVE_REVIEWS = 4;
    const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;
    
    type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };
    type OperationsAccount = BoardSignalAccount & {
      originalBetaPlayer?: boolean;
      originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;
      reviewHistoryBackfill?: ReviewHistoryBackfillState;
    };
    type StoredReview = {
      deskKey?: string;
      periodEnd?: string;
      publishedAt?: string;
      importedAt?: string;
      reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";
      countsTowardRetention?: boolean;
      summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };
      desk?: { source?: string; provenance?: { verified?: boolean } };
      originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };
    };
    type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };
    type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };
    
    function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }
    function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }
    function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }
    function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }
    function historicalPublishedPeriods(account: OperationsAccount) {
      return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;
    }
    function validPeriod(data: StoredReview): ReviewPeriod | undefined {
      const history = data.originalBeta?.history;
      const start = history?.periodStart ?? data.summary?.periodStart;
      const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;
      if (!start || !end) return undefined;
      const lifecycle = storedReviewLifecycle(data);
      const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;
      return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;
    }
    
    function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {
      if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };
      return undefined;
    }
    
    async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {
      const db = getAdminDb();
      const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();
      const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));
      const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });
      const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));
      const latestNonHistorical = documents.flatMap(({ data }) => {
        const period = validPeriod(data);
        return period && period.source !== "historical" ? [{ data, period }] : [];
      })[0];
      const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });
      const liveHistory = documents.flatMap(({ id, data }) => {
        const period = validPeriod(data);
        if (!period || period.source === "historical" || data.originalBeta?.history) return [];
        return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];
      });
      const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));
    
      const [unread, coverage, reportTruth, exceptions] = await Promise.all([
        db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),
        db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),
        loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),
        db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),
      ]);
      const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);
      const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;
      const unreadReplies = Number((unread as { size?: number }).size ?? 0);
      const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";
      const derived = deriveFounderOperation({
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        preferredContactValue: account.preferredContactValue,
        lastSeenAt: account.lastSeenAt,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        forming: cadenceWeekForming(account),
        latestReview,
        unreadReplies,
        unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionCount: activeExceptions.length,
        identityConflict: identityConflict(account),
        founderOps: account.founderOps,
      }, now);
      const officialChessState = latestOfficialChessStatePeriod(periods);
      const row: FounderOperationsRow = {
        ...derived,
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        playerId: account.chessCom.playerId,
        profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,
        avatar: account.chessCom.avatar,
        reviewCount: nonHistorical.length,
        reviewPeriods: nonHistorical,
        history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),
        latestReview,
        latestReportPeriod: reportTruth.periods.at(-1),
        recentReportPeriods: reportTruth.periods,
        historyCoverage: reportTruth.coverage,
        officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        lastSeenAt: account.lastSeenAt,
        unreadReplies,
        exceptionCount: activeExceptions.length,
        exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),
        identityConflict: identityConflict(account),
        pendingRequest: false,
        forming: derived.forming,
        preferredContactMethod: account.preferredContactMethod,
        preferredContactValue: account.preferredContactValue,
        accountStatus: account.accessStatus,
        accessStatus: account.accessStatus,
        identityStatus: account.identityStatus,
        identityReviewStatus: account.identityReviewStatus,
        publicHighlightsStatus,
        lastContactedAt: account.founderOps?.lastContactedAt,
        lastContactMethod: account.founderOps?.lastContactMethod,
        followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,
      };
      const originalReviews = Math.max(
        nonHistorical.filter((period) => period.source === "original").length,
        Object.keys(account.originalBetaHistoryPeriods ?? {}).length,
        account.originalBetaPlayer ? 1 : 0,
      );
      const liveReviews = Math.max(
        account.reviewProduction?.organicLiveReviews ?? 0,
        nonHistorical.filter((period) => period.source === "live").length,
      );
      const historicalPeriods = Math.max(
        account.reviewProduction?.historicalBackfillReviews ?? 0,
        historicalPublishedPeriods(account),
      );
      return founderSummaryFromRow({
        playerId: account.chessCom.playerId,
        uid: account.uid,
        row,
        originalReviews,
        liveReviews,
        historicalPeriods,
        reviewProduction: account.reviewProduction,
        activationBaseline: account.reviewHistoryBackfill?.activationBaseline === true,
        updatedAt: now.toISOString(),
      });
    }
    
    export async function refreshFounderPlayerSummary(account: OperationsAccount, now = new Date()) {
      if (account.role !== "player") return;
      const summary = await sourcePlayerSummary(account, now);
      await upsertFounderPlayerSummary(summary, now);
      return summary;
    }
    
    export async function refreshFounderPlayerSummaryByUid(uid: string, now = new Date()) {
      const snapshot = await getAdminDb().collection("users").doc(uid).get();
      if (!snapshot.exists) return;
      return refreshFounderPlayerSummary(snapshot.data() as OperationsAccount, now);
    }
    
    function bootstrapLeaseRef() { return getAdminDb().collection("founderOperationsState").doc("bootstrapLease"); }
    async function acquireBootstrapLease(now = new Date()) {
      const db = getAdminDb();
      return db.runTransaction(async (transaction) => {
        const [current, lease] = await Promise.all([transaction.get(db.collection("founderOperationsState").doc("current")), transaction.get(bootstrapLeaseRef())]);
        if (current.exists && current.data()?.version === FOUNDER_MATERIALIZED_VERSION && current.data()?.bootstrapComplete === true) return false;
        const data = lease.data() as { leaseUntil?: string } | undefined;
        if (data?.leaseUntil && data.leaseUntil > now.toISOString()) return false;
        transaction.set(bootstrapLeaseRef(), { leaseUntil: new Date(now.getTime() + FOUNDER_BOOTSTRAP_LEASE_MS).toISOString(), startedAt: now.toISOString() }, { merge: false });
        return true;
      });
    }
    
    async function bootstrapFounderMaterialization(now = new Date()) {
      if (!await acquireBootstrapLease(now)) return undefined;
      try {
        const db = getAdminDb();
        const [users, requests] = await Promise.all([db.collection("users").get(), db.collection("betaRequests").get()]);
        const accounts = users.docs.map((document) => document.data() as OperationsAccount).filter((account) => account.role === "player");
        for (const account of accounts) await refreshFounderPlayerSummary(account, now);
        for (const document of requests.docs) {
          const request = { id: document.id, ...document.data() } as PendingRequest;
          if (request.status === "pending") await upsertFounderPendingRequestSummary(founderPendingSummaryFromInput(request, now), now);
        }
        const aggregate = await rebuildFounderAggregateFromSummaries(now);
        await bootstrapLeaseRef().delete().catch(() => undefined);
        return aggregate;
      } catch (error) {
        await bootstrapLeaseRef().delete().catch(() => undefined);
        throw error;
      }
    }
    
    export async function founderOperationsSnapshot(now = new Date(), includeRows = false) {
      let aggregate = await loadFounderOperationsAggregate(now);
      if (!aggregate) {
        aggregate = await bootstrapFounderMaterialization(now);
        if (!aggregate) throw Object.assign(new Error("Founder operations are being prepared. Try again shortly."), { status: 503, code: "FOUNDER_OPERATIONS_BOOTSTRAPPING", retryAfterSeconds: 5 });
      }
      return { ...aggregate, rows: includeRows ? await loadFounderOperationRows() : [] };
    }
    
    async function accountForFounderOps(uidInput: unknown) {
      const uid = String(uidInput ?? "").trim();
      if (!uid || uid.startsWith("request:") || uid.includes("/")) throw Object.assign(new Error("A valid BoardSignal player is required."), { status: 400 });
      const ref = getAdminDb().collection("users").doc(uid);
      const snapshot = await ref.get();
      if (!snapshot.exists) throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      const account = snapshot.data() as OperationsAccount;
      if (account.role !== "player") throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      return { ref, account };
    }
    
    export async function markFounderContacted(uidInput: unknown, methodInput: unknown, now = new Date()) {
      const method = String(methodInput ?? "") as BoardSignalFounderContactMethod;
      if (!["email", "discord", "telegram", "chesscom", "other"].includes(method)) throw Object.assign(new Error("Choose a valid contact method."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), lastContactedAt: now.toISOString(), lastContactMethod: method };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function snoozeFounderFollowUp(uidInput: unknown, daysInput: unknown, now = new Date()) {
      const days = Number(daysInput);
      if (![1, 3, 7].includes(days)) throw Object.assign(new Error("Choose a 1, 3, or 7 day snooze."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), followUpSnoozedUntil: new Date(now.getTime() + days * 24 * 60 * 60 * 1000).toISOString() };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function clearFounderFollowUpSnooze(uidInput: unknown, now = new Date()) {
      const { ref, account } = await accountForFounderOps(uidInput);
      await ref.update({ "founderOps.followUpSnoozedUntil": FieldValue.delete() });
      const founderOps = { ...(account.founderOps ?? {}) };
      delete founderOps.followUpSnoozedUntil;
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return { cleared: true };
    }
    
    export function normalizeFounderUsername(value?: string) { return normalize(value); }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:199:22)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: H.1 13 pocket-card combined cascade owns readable inverse surface
ok 87 - H.1 13 pocket-card combined cascade owns readable inverse surface
  ---
  duration_ms: 0.475213
  type: 'test'
  ...
# Subtest: H.1 14 dark-mode Profile participation and controls are semantic
ok 88 - H.1 14 dark-mode Profile participation and controls are semantic
  ---
  duration_ms: 0.363652
  type: 'test'
  ...
# Subtest: H.1 15 customer-facing Founding Beta Field maps to Founding Access
ok 89 - H.1 15 customer-facing Founding Beta Field maps to Founding Access
  ---
  duration_ms: 0.198995
  type: 'test'
  ...
# Subtest: H.1 16 internal founding_beta values remain intact
not ok 90 - H.1 16 internal founding_beta values remain intact
  ---
  duration_ms: 0.503341
  type: 'test'
  location: '/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:234:25'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /account\.accessTier === "founding_beta"/. Input:
    
    'import "server-only";\n' +
      '\n' +
      'import { FieldValue } from "firebase-admin/firestore";\n' +
      'import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";\n' +
      'import {\n' +
      '  deriveFounderOperation,\n' +
      '  summarizeFounderHistoryVisibility,\n' +
      '} from "../founderOperationsLogic";\n' +
      'import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";\n' +
      'import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";\n' +
      'import type { CompletedReviewHistoryItem } from "../reviewHistory";\n' +
      'import { getAdminDb } from "../../../utils/firebaseAdmin";\n' +
      'import { loadRecentReportPeriodTruth } from "./reviewPeriods";\n' +
      'import {\n' +
      '  FOUNDER_MATERIALIZED_VERSION,\n' +
      '  founderPendingSummaryFromInput,\n' +
      '  founderSummaryFromRow,\n' +
      '  loadFounderOperationRows,\n' +
      '  loadFounderOperationsAggregate,\n' +
      '  rebuildFounderAggregateFromSummaries,\n' +
      '  upsertFounderPendingRequestSummary,\n' +
      '  upsertFounderPlayerSummary,\n' +
      '  type FounderOperationsRow,\n' +
      '} from "./founderMaterialized";\n' +
      '\n' +
      'const MAX_ACTIVE_REVIEWS = 4;\n' +
      'const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;\n' +
      '\n' +
      'type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };\n' +
      'type OperationsAccount = BoardSignalAccount & {\n' +
      '  originalBetaPlayer?: boolean;\n' +
      '  originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;\n' +
      '  reviewHistoryBackfill?: ReviewHistoryBackfillState;\n' +
      '};\n' +
      'type StoredReview = {\n' +
      '  deskKey?: string;\n' +
      '  periodEnd?: string;\n' +
      '  publishedAt?: string;\n' +
      '  importedAt?: string;\n' +
      '  reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";\n' +
      '  countsTowardRetention?: boolean;\n' +
      '  summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };\n' +
      '  desk?: { source?: string; provenance?: { verified?: boolean } };\n' +
      '  originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };\n' +
      '};\n' +
      'type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };\n' +
      'type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };\n' +
      '\n' +
      'function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }\n' +
      'function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }\n' +
      'function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }\n' +
      'function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }\n' +
      'function historicalPublishedPeriods(account: OperationsAccount) {\n' +
      '  return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;\n' +
      '}\n' +
      'function validPeriod(data: StoredReview): ReviewPeriod | undefined {\n' +
      '  const history = data.originalBeta?.history;\n' +
      '  const start = history?.periodStart ?? data.summary?.periodStart;\n' +
      '  const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;\n' +
      '  if (!start || !end) return undefined;\n' +
      '  const lifecycle = storedReviewLifecycle(data);\n' +
      '  const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;\n' +
      '  return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;\n' +
      '}\n' +
      '\n' +
      'function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {\n' +
      '  if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };\n' +
      '  return undefined;\n' +
      '}\n' +
      '\n' +
      'async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {\n' +
      '  const db = getAdminDb();\n' +
      '  const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();\n' +
      '  const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));\n' +
      '  const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });\n' +
      '  const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));\n' +
      '  const latestNonHistorical = documents.flatMap(({ data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    return period && period.source !== "historical" ? [{ data, period }] : [];\n' +
      '  })[0];\n' +
      '  const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });\n' +
      '  const liveHistory = documents.flatMap(({ id, data }) => {\n' +
      '    const period = validPeriod(data);\n' +
      '    if (!period || period.source === "historical" || data.originalBeta?.history) return [];\n' +
      '    return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];\n' +
      '  });\n' +
      '  const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));\n' +
      '\n' +
      '  const [unread, coverage, reportTruth, exceptions] = await Promise.all([\n' +
      '    db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),\n' +
      '    db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),\n' +
      '    loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),\n' +
      '    db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),\n' +
      '  ]);\n' +
      '  const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);\n' +
      '  const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;\n' +
      '  const unreadReplies = Number((unread as { size?: number }).size ?? 0);\n' +
      '  const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";\n' +
      '  const derived = deriveFounderOperation({\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    forming: cadenceWeekForming(account),\n' +
      '    latestReview,\n' +
      '    unreadReplies,\n' +
      '    unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    founderOps: account.founderOps,\n' +
      '  }, now);\n' +
      '  const officialChessState = latestOfficialChessStatePeriod(periods);\n' +
      '  const row: FounderOperationsRow = {\n' +
      '    ...derived,\n' +
      '    uid: account.uid,\n' +
      '    username: account.chessCom.canonicalUsername,\n' +
      '    playerId: account.chessCom.playerId,\n' +
      '    profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,\n' +
      '    avatar: account.chessCom.avatar,\n' +
      '    reviewCount: nonHistorical.length,\n' +
      '    reviewPeriods: nonHistorical,\n' +
      '    history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),\n' +
      '    latestReview,\n' +
      '    latestReportPeriod: reportTruth.periods.at(-1),\n' +
      '    recentReportPeriods: reportTruth.periods,\n' +
      '    historyCoverage: reportTruth.coverage,\n' +
      '    officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,\n' +
      '    nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,\n' +
      '    lastSeenAt: account.lastSeenAt,\n' +
      '    unreadReplies,\n' +
      '    exceptionCount: activeExceptions.length,\n' +
      '    exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),\n' +
      '    identityConflict: identityConflict(account),\n' +
      '    pendingRequest: false,\n' +
      '    forming: derived.forming,\n' +
      '    preferredContactMethod: account.preferredContactMethod,\n' +
      '    preferredContactValue: account.preferredContactValue,\n' +
      '    accountStatus: account.accessStatus,\n' +
      '    accessStatus: account.accessStatus,\n' +
      '    identityStatus: account.identityStatus,\n' +
      '    identityReviewStatus: account.identityReviewStatus,\n' +
      '    publicHighlightsStatus,\n' +
      '    lastContactedAt: account.founderOps?.lastContactedAt,\n' +
      '    lastContactMethod: account.founderOps?.lastContactMethod,\n' +
      '    followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,\n' +
      '  };\n' +
      '  const originalReviews = Math.max(\n' +
      '    nonHistorical.filter((period) => period.source === "original").length,\n' +
      '    Object.keys(account.originalBetaHistoryPeriods ?? {}).length,\n' +
      '    account.originalBetaPlayer ? 1 : 0,\n' +
      '  );\n' +
      '  const liveReviews = Math.max(\n' +
      '    account.reviewProduction?.organicLiveReviews ?? 0,\n' +
      '    nonHistorical.fi'... 6028 more characters
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import "server-only";
    
    import { FieldValue } from "firebase-admin/firestore";
    import type { BoardSignalAccount, BoardSignalFounderContactMethod } from "../account";
    import {
      deriveFounderOperation,
      summarizeFounderHistoryVisibility,
    } from "../founderOperationsLogic";
    import { storedReviewLifecycle, type ReviewHistoryBackfillState } from "../historyBackfill";
    import { latestOfficialChessStatePeriod, type CanonicalReportPeriod } from "../reviewPeriods";
    import type { CompletedReviewHistoryItem } from "../reviewHistory";
    import { getAdminDb } from "../../../utils/firebaseAdmin";
    import { loadRecentReportPeriodTruth } from "./reviewPeriods";
    import {
      FOUNDER_MATERIALIZED_VERSION,
      founderPendingSummaryFromInput,
      founderSummaryFromRow,
      loadFounderOperationRows,
      loadFounderOperationsAggregate,
      rebuildFounderAggregateFromSummaries,
      upsertFounderPendingRequestSummary,
      upsertFounderPlayerSummary,
      type FounderOperationsRow,
    } from "./founderMaterialized";
    
    const MAX_ACTIVE_REVIEWS = 4;
    const FOUNDER_BOOTSTRAP_LEASE_MS = 60_000;
    
    type OriginalBetaMarker = { periodStart?: string; periodEnd?: string; retiredAt?: string };
    type OperationsAccount = BoardSignalAccount & {
      originalBetaPlayer?: boolean;
      originalBetaHistoryPeriods?: Record<string, OriginalBetaMarker>;
      reviewHistoryBackfill?: ReviewHistoryBackfillState;
    };
    type StoredReview = {
      deskKey?: string;
      periodEnd?: string;
      publishedAt?: string;
      importedAt?: string;
      reviewLifecycle?: "organic_live" | "historical_backfill" | "original_beta";
      countsTowardRetention?: boolean;
      summary?: { deskKey?: string; periodStart?: string; periodEnd?: string; periodLabel?: string };
      desk?: { source?: string; provenance?: { verified?: boolean } };
      originalBeta?: { seedHandle?: string; history?: CompletedReviewHistoryItem };
    };
    type PendingRequest = { id: string; chessPlayerId?: number; canonicalUsername?: string; preferredContactMethod?: string; preferredContactValue?: string; requestedAt?: string; status?: string; profileUrl?: string };
    type ReviewPeriod = { periodStart: string; periodEnd: string; periodLabel?: string; source: "original" | "live" | "historical" };
    
    function clean<T>(value: T): T { return JSON.parse(JSON.stringify(value)) as T; }
    function normalize(value?: string) { return String(value ?? "").trim().replace(/^@/, "").toLowerCase(); }
    function identityConflict(account: OperationsAccount) { return account.identityStatus === "revoked" || account.identityReviewStatus === "rejected"; }
    function cadenceWeekForming(account: OperationsAccount) { return account.accessStatus === "active" && Boolean(account.cadenceAnchor); }
    function historicalPublishedPeriods(account: OperationsAccount) {
      return Object.values(account.reviewHistoryBackfill?.evaluated ?? {}).filter((evaluation) => evaluation?.status === "published").length;
    }
    function validPeriod(data: StoredReview): ReviewPeriod | undefined {
      const history = data.originalBeta?.history;
      const start = history?.periodStart ?? data.summary?.periodStart;
      const end = history?.periodEnd ?? data.summary?.periodEnd ?? data.periodEnd;
      if (!start || !end) return undefined;
      const lifecycle = storedReviewLifecycle(data);
      const source = history ? "original" as const : lifecycle === "historical_backfill" ? "historical" as const : lifecycle === "organic_live" ? "live" as const : undefined;
      return source ? { periodStart: start, periodEnd: end, periodLabel: history?.periodLabel ?? data.summary?.periodLabel, source } : undefined;
    }
    
    function historyItemFromStored(documentId: string, data: StoredReview): CompletedReviewHistoryItem | undefined {
      if (data.originalBeta?.history) return { ...data.originalBeta.history, reviewKey: String(data.originalBeta.history.reviewKey ?? documentId), reviewLifecycle: "original_beta" };
      return undefined;
    }
    
    async function sourcePlayerSummary(account: OperationsAccount, now = new Date()) {
      const db = getAdminDb();
      const desksSnapshot = await db.collection("users").doc(account.uid).collection("desks").orderBy("periodEnd", "desc").limit(MAX_ACTIVE_REVIEWS).get();
      const documents = desksSnapshot.docs.map((document) => ({ id: document.id, data: document.data() as StoredReview }));
      const periods = documents.flatMap(({ data }) => { const period = validPeriod(data); return period ? [period] : []; });
      const nonHistorical = periods.filter((period) => period.source !== "historical").map((period) => ({ ...period, source: period.source as "original" | "live" }));
      const latestNonHistorical = documents.flatMap(({ data }) => {
        const period = validPeriod(data);
        return period && period.source !== "historical" ? [{ data, period }] : [];
      })[0];
      const originalHistory = documents.flatMap(({ id, data }) => { const item = historyItemFromStored(id, data); return item ? [item] : []; });
      const liveHistory = documents.flatMap(({ id, data }) => {
        const period = validPeriod(data);
        if (!period || period.source === "historical" || data.originalBeta?.history) return [];
        return [{ reviewKey: String(data.deskKey ?? id), periodStart: period.periodStart, periodEnd: period.periodEnd, periodLabel: period.periodLabel ?? `${period.periodStart} → ${period.periodEnd}`, reviewLifecycle: "organic_live" as const, source: "live" as const } as CompletedReviewHistoryItem];
      });
      const completedHistory = [...originalHistory, ...liveHistory].sort((a, b) => b.periodEnd.localeCompare(a.periodEnd));
    
      const [unread, coverage, reportTruth, exceptions] = await Promise.all([
        db.collection("users").doc(account.uid).collection("conversations").where("unreadForFounder", "==", true).get().catch(() => ({ size: 0, docs: [] })),
        db.collection("publicCoverage").where("chessPlayerId", "==", String(account.chessCom.playerId)).get().catch(() => ({ size: 0 })),
        loadRecentReportPeriodTruth(account, now, false, completedHistory).catch(() => ({ periods: [] as CanonicalReportPeriod[], coverage: { evaluatedCount: 0, totalCount: 0 } })),
        db.collection("exceptions").where("uid", "==", account.uid).limit(20).get().catch(() => ({ docs: [] })),
      ]);
      const activeExceptions = (exceptions as { docs: Array<{ data(): { title?: string; message?: string; resolvedAt?: string; createdAt?: string } }> }).docs.map((document) => document.data()).filter((item) => !item.resolvedAt);
      const latestReview = latestNonHistorical ? { deskKey: latestNonHistorical.data.deskKey, periodStart: latestNonHistorical.period.periodStart, periodEnd: latestNonHistorical.period.periodEnd, periodLabel: latestNonHistorical.period.periodLabel, publishedAt: latestNonHistorical.data.publishedAt } : undefined;
      const unreadReplies = Number((unread as { size?: number }).size ?? 0);
      const publicHighlightsStatus = account.identityStatus === "provisional" ? "waiting_identity_review" : periods.length === 0 ? "no_completed_review" : Number((coverage as { size?: number }).size ?? 0) > 0 ? "live" : "repair_needed";
      const derived = deriveFounderOperation({
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        preferredContactValue: account.preferredContactValue,
        lastSeenAt: account.lastSeenAt,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        forming: cadenceWeekForming(account),
        latestReview,
        unreadReplies,
        unreadReplyAt: (unread as { docs?: Array<{ data(): { updatedAt?: string } }> }).docs?.map((document) => document.data().updatedAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionAt: activeExceptions.map((item) => item.createdAt).filter((value): value is string => Boolean(value)).sort()[0],
        exceptionCount: activeExceptions.length,
        identityConflict: identityConflict(account),
        founderOps: account.founderOps,
      }, now);
      const officialChessState = latestOfficialChessStatePeriod(periods);
      const row: FounderOperationsRow = {
        ...derived,
        uid: account.uid,
        username: account.chessCom.canonicalUsername,
        playerId: account.chessCom.playerId,
        profileUrl: account.chessCom.profileUrl || `https://www.chess.com/member/${encodeURIComponent(account.chessCom.canonicalUsername)}`,
        avatar: account.chessCom.avatar,
        reviewCount: nonHistorical.length,
        reviewPeriods: nonHistorical,
        history: summarizeFounderHistoryVisibility(account.reviewHistoryBackfill, periods.map((period) => period.periodStart)),
        latestReview,
        latestReportPeriod: reportTruth.periods.at(-1),
        recentReportPeriods: reportTruth.periods,
        historyCoverage: reportTruth.coverage,
        officialChessStatePeriod: officialChessState ? { periodStart: officialChessState.periodStart, periodEnd: officialChessState.periodEnd, periodLabel: officialChessState.periodLabel } : undefined,
        nextDeskDueAt: account.nextDeskDueAt ?? account.currentEpisodeSummary?.nextDeskDueAt,
        lastSeenAt: account.lastSeenAt,
        unreadReplies,
        exceptionCount: activeExceptions.length,
        exceptionTitles: activeExceptions.map((item) => item.title ?? item.message ?? "BoardSignal exception"),
        identityConflict: identityConflict(account),
        pendingRequest: false,
        forming: derived.forming,
        preferredContactMethod: account.preferredContactMethod,
        preferredContactValue: account.preferredContactValue,
        accountStatus: account.accessStatus,
        accessStatus: account.accessStatus,
        identityStatus: account.identityStatus,
        identityReviewStatus: account.identityReviewStatus,
        publicHighlightsStatus,
        lastContactedAt: account.founderOps?.lastContactedAt,
        lastContactMethod: account.founderOps?.lastContactMethod,
        followUpSnoozedUntil: account.founderOps?.followUpSnoozedUntil,
      };
      const originalReviews = Math.max(
        nonHistorical.filter((period) => period.source === "original").length,
        Object.keys(account.originalBetaHistoryPeriods ?? {}).length,
        account.originalBetaPlayer ? 1 : 0,
      );
      const liveReviews = Math.max(
        account.reviewProduction?.organicLiveReviews ?? 0,
        nonHistorical.filter((period) => period.source === "live").length,
      );
      const historicalPeriods = Math.max(
        account.reviewProduction?.historicalBackfillReviews ?? 0,
        historicalPublishedPeriods(account),
      );
      return founderSummaryFromRow({
        playerId: account.chessCom.playerId,
        uid: account.uid,
        row,
        originalReviews,
        liveReviews,
        historicalPeriods,
        reviewProduction: account.reviewProduction,
        activationBaseline: account.reviewHistoryBackfill?.activationBaseline === true,
        updatedAt: now.toISOString(),
      });
    }
    
    export async function refreshFounderPlayerSummary(account: OperationsAccount, now = new Date()) {
      if (account.role !== "player") return;
      const summary = await sourcePlayerSummary(account, now);
      await upsertFounderPlayerSummary(summary, now);
      return summary;
    }
    
    export async function refreshFounderPlayerSummaryByUid(uid: string, now = new Date()) {
      const snapshot = await getAdminDb().collection("users").doc(uid).get();
      if (!snapshot.exists) return;
      return refreshFounderPlayerSummary(snapshot.data() as OperationsAccount, now);
    }
    
    function bootstrapLeaseRef() { return getAdminDb().collection("founderOperationsState").doc("bootstrapLease"); }
    async function acquireBootstrapLease(now = new Date()) {
      const db = getAdminDb();
      return db.runTransaction(async (transaction) => {
        const [current, lease] = await Promise.all([transaction.get(db.collection("founderOperationsState").doc("current")), transaction.get(bootstrapLeaseRef())]);
        if (current.exists && current.data()?.version === FOUNDER_MATERIALIZED_VERSION && current.data()?.bootstrapComplete === true) return false;
        const data = lease.data() as { leaseUntil?: string } | undefined;
        if (data?.leaseUntil && data.leaseUntil > now.toISOString()) return false;
        transaction.set(bootstrapLeaseRef(), { leaseUntil: new Date(now.getTime() + FOUNDER_BOOTSTRAP_LEASE_MS).toISOString(), startedAt: now.toISOString() }, { merge: false });
        return true;
      });
    }
    
    async function bootstrapFounderMaterialization(now = new Date()) {
      if (!await acquireBootstrapLease(now)) return undefined;
      try {
        const db = getAdminDb();
        const [users, requests] = await Promise.all([db.collection("users").get(), db.collection("betaRequests").get()]);
        const accounts = users.docs.map((document) => document.data() as OperationsAccount).filter((account) => account.role === "player");
        for (const account of accounts) await refreshFounderPlayerSummary(account, now);
        for (const document of requests.docs) {
          const request = { id: document.id, ...document.data() } as PendingRequest;
          if (request.status === "pending") await upsertFounderPendingRequestSummary(founderPendingSummaryFromInput(request, now), now);
        }
        const aggregate = await rebuildFounderAggregateFromSummaries(now);
        await bootstrapLeaseRef().delete().catch(() => undefined);
        return aggregate;
      } catch (error) {
        await bootstrapLeaseRef().delete().catch(() => undefined);
        throw error;
      }
    }
    
    export async function founderOperationsSnapshot(now = new Date(), includeRows = false) {
      let aggregate = await loadFounderOperationsAggregate(now);
      if (!aggregate) {
        aggregate = await bootstrapFounderMaterialization(now);
        if (!aggregate) throw Object.assign(new Error("Founder operations are being prepared. Try again shortly."), { status: 503, code: "FOUNDER_OPERATIONS_BOOTSTRAPPING", retryAfterSeconds: 5 });
      }
      return { ...aggregate, rows: includeRows ? await loadFounderOperationRows() : [] };
    }
    
    async function accountForFounderOps(uidInput: unknown) {
      const uid = String(uidInput ?? "").trim();
      if (!uid || uid.startsWith("request:") || uid.includes("/")) throw Object.assign(new Error("A valid BoardSignal player is required."), { status: 400 });
      const ref = getAdminDb().collection("users").doc(uid);
      const snapshot = await ref.get();
      if (!snapshot.exists) throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      const account = snapshot.data() as OperationsAccount;
      if (account.role !== "player") throw Object.assign(new Error("The BoardSignal player account was not found."), { status: 404 });
      return { ref, account };
    }
    
    export async function markFounderContacted(uidInput: unknown, methodInput: unknown, now = new Date()) {
      const method = String(methodInput ?? "") as BoardSignalFounderContactMethod;
      if (!["email", "discord", "telegram", "chesscom", "other"].includes(method)) throw Object.assign(new Error("Choose a valid contact method."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), lastContactedAt: now.toISOString(), lastContactMethod: method };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function snoozeFounderFollowUp(uidInput: unknown, daysInput: unknown, now = new Date()) {
      const days = Number(daysInput);
      if (![1, 3, 7].includes(days)) throw Object.assign(new Error("Choose a 1, 3, or 7 day snooze."), { status: 400 });
      const { ref, account } = await accountForFounderOps(uidInput);
      const founderOps = { ...(account.founderOps ?? {}), followUpSnoozedUntil: new Date(now.getTime() + days * 24 * 60 * 60 * 1000).toISOString() };
      await ref.set({ founderOps }, { merge: true });
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return founderOps;
    }
    
    export async function clearFounderFollowUpSnooze(uidInput: unknown, now = new Date()) {
      const { ref, account } = await accountForFounderOps(uidInput);
      await ref.update({ "founderOps.followUpSnoozedUntil": FieldValue.delete() });
      const founderOps = { ...(account.founderOps ?? {}) };
      delete founderOps.followUpSnoozedUntil;
      await refreshFounderPlayerSummary({ ...account, founderOps }, now);
      return { cleared: true };
    }
    
    export function normalizeFounderUsername(value?: string) { return normalize(value); }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:236:22)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: H.1 17 protected G.3 Player Room hierarchy remains intact
ok 91 - H.1 17 protected G.3 Player Room hierarchy remains intact
  ---
  duration_ms: 0.172015
  type: 'test'
  ...
# Subtest: H.1 18 protected F.2 readability contract remains intact
not ok 92 - H.1 18 protected F.2 readability contract remains intact
  ---
  duration_ms: 0.490353
  type: 'test'
  location: '/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:248:25'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /boardsignal-h1-hotfix\.css";\nimport "\.\/boardsignal-f2-readability\.css";/. Input:
    
    'import type { Metadata, Viewport } from "next";\n' +
      'import { Inter, Montserrat } from "next/font/google";\n' +
      'import "./globals.css";\n' +
      'import "./boardsignal-foundation.css";\n' +
      'import "./boardsignal-accessibility.css";\n' +
      'import "./boardsignal-motion.css";\n' +
      'import "./boardsignal-player-room-g3.css";\n' +
      'import "./boardsignal-h1-hotfix.css";\n' +
      'import "./boardsignal-g4-universe.css";\n' +
      'import "./boardsignal-g41-weekly-truth.css";\n' +
      'import "./boardsignal-f2-readability.css";\n' +
      '\n' +
      'import ServiceWorkerRegister from "@/components/ServiceWorkerRegister";\n' +
      'import TrustpilotInvitationBridge from "@/components/TrustpilotInvitationBridge";\n' +
      'import { AnalyticsProvider } from "@/components/AnalyticsProvider";\n' +
      'import { Analytics } from "@vercel/analytics/next";\n' +
      'import { SpeedInsights } from "@vercel/speed-insights/next";\n' +
      'import ConnectivityProvider from "@/components/ConnectivityProvider";\n' +
      'import RouteAwarePublicChrome from "@/components/RouteAwarePublicChrome";\n' +
      '\n' +
      'const montserrat = Montserrat({\n' +
      '  variable: "--font-sans",\n' +
      '  subsets: ["latin"],\n' +
      '  weight: ["400", "500", "600", "700", "800"],\n' +
      '  display: "swap",\n' +
      '});\n' +
      '\n' +
      'const inter = Inter({\n' +
      '  variable: "--font-ui",\n' +
      '  subsets: ["latin"],\n' +
      '  weight: ["400", "500", "600", "700", "800"],\n' +
      '  display: "swap",\n' +
      '});\n' +
      '\n' +
      'export const metadata: Metadata = {\n' +
      '  title: {\n' +
      '    default: "BoardSignal — Your weekly chess Review",\n' +
      '    template: "%s | BoardSignal",\n' +
      '  },\n' +
      '  description: "BoardSignal turns a fixed seven days of your Chess.com games into a factual Review, a clear signal and a plan you can use.",\n' +
      '  applicationName: "BoardSignal",\n' +
      '  keywords: ["chess improvement", "Chess.com analysis", "weekly chess report", "chess insights", "BoardSignal"],\n' +
      '  manifest: "/manifest.webmanifest",\n' +
      '  openGraph: {\n' +
      '    title: "BoardSignal — Your games, covered like sport",\n' +
      '    description: "Personal sports coverage and private performance guidance for everyday chess players.",\n' +
      '    siteName: "BoardSignal",\n' +
      '    type: "website",\n' +
      '  },\n' +
      '};\n' +
      '\n' +
      'export const viewport: Viewport = {\n' +
      '  width: "device-width",\n' +
      '  initialScale: 1,\n' +
      '  viewportFit: "cover",\n' +
      '  themeColor: [\n' +
      '    { media: "(prefers-color-scheme: light)", color: "#f4f0e7" },\n' +
      '    { media: "(prefers-color-scheme: dark)", color: "#0b1220" },\n' +
      '  ],\n' +
      '};\n' +
      '\n' +
      'const boardSignalThemeBootstrap = `(() => {\n' +
      '  const key = "boardsignal:theme";\n' +
      '  let choice = "system";\n' +
      '  try {\n' +
      '    const stored = window.localStorage.getItem(key);\n' +
      '    if (stored === "light" || stored === "dark" || stored === "system") choice = stored;\n' +
      '  } catch {}\n' +
      '  const dark = choice === "dark" || (choice === "system" && window.matchMedia("(prefers-color-scheme: dark)").matches);\n' +
      '  const root = document.documentElement;\n' +
      '  root.dataset.bsTheme = dark ? "dark" : "light";\n' +
      '  root.dataset.bsThemeChoice = choice;\n' +
      '  root.style.colorScheme = dark ? "dark" : "light";\n' +
      '})();`;\n' +
      '\n' +
      'const trustpilotInvitationBootstrap = `(function(w,d,s,r,n){\n' +
      '  w.TrustpilotObject=n;\n' +
      '  w[n]=w[n]||function(){(w[n].q=w[n].q||[]).push(arguments)};\n' +
      "  var a=d.createElement(s);a.async=1;a.src=r;a.type='text/java'+s;\n" +
      '  var f=d.getElementsByTagName(s)[0];f.parentNode.insertBefore(a,f);\n' +
      "})(window,document,'script','https://invitejs.trustpilot.com/tp.min.js','tp');\n" +
      "tp('register','oiloKbZhU5G7rp2L');`;\n" +
      '\n' +
      'export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {\n' +
      '  return (\n' +
      '    <html lang="en" className={`${montserrat.variable} ${inter.variable}`} suppressHydrationWarning>\n' +
      '      <head>\n' +
      '        <meta name="trustpilot-one-time-domain-verification-id" content="88f344e5-4f9b-4089-b887-1fb2bdbababb" />\n' +
      '        <script dangerouslySetInnerHTML={{ __html: boardSignalThemeBootstrap }} />\n' +
      '        <script dangerouslySetInnerHTML={{ __html: trustpilotInvitationBootstrap }} />\n' +
      '      </head>\n' +
      '      <body suppressHydrationWarning>\n' +
      '        <TrustpilotInvitationBridge />\n' +
      '        <AnalyticsProvider>\n' +
      '          <ConnectivityProvider>\n' +
      '            <RouteAwarePublicChrome>{children}</RouteAwarePublicChrome>\n' +
      '            <ServiceWorkerRegister />\n' +
      '            <Analytics />\n' +
      '            <SpeedInsights />\n' +
      '          </ConnectivityProvider>\n' +
      '        </AnalyticsProvider>\n' +
      '      </body>\n' +
      '    </html>\n' +
      '  );\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import type { Metadata, Viewport } from "next";
    import { Inter, Montserrat } from "next/font/google";
    import "./globals.css";
    import "./boardsignal-foundation.css";
    import "./boardsignal-accessibility.css";
    import "./boardsignal-motion.css";
    import "./boardsignal-player-room-g3.css";
    import "./boardsignal-h1-hotfix.css";
    import "./boardsignal-g4-universe.css";
    import "./boardsignal-g41-weekly-truth.css";
    import "./boardsignal-f2-readability.css";
    
    import ServiceWorkerRegister from "@/components/ServiceWorkerRegister";
    import TrustpilotInvitationBridge from "@/components/TrustpilotInvitationBridge";
    import { AnalyticsProvider } from "@/components/AnalyticsProvider";
    import { Analytics } from "@vercel/analytics/next";
    import { SpeedInsights } from "@vercel/speed-insights/next";
    import ConnectivityProvider from "@/components/ConnectivityProvider";
    import RouteAwarePublicChrome from "@/components/RouteAwarePublicChrome";
    
    const montserrat = Montserrat({
      variable: "--font-sans",
      subsets: ["latin"],
      weight: ["400", "500", "600", "700", "800"],
      display: "swap",
    });
    
    const inter = Inter({
      variable: "--font-ui",
      subsets: ["latin"],
      weight: ["400", "500", "600", "700", "800"],
      display: "swap",
    });
    
    export const metadata: Metadata = {
      title: {
        default: "BoardSignal — Your weekly chess Review",
        template: "%s | BoardSignal",
      },
      description: "BoardSignal turns a fixed seven days of your Chess.com games into a factual Review, a clear signal and a plan you can use.",
      applicationName: "BoardSignal",
      keywords: ["chess improvement", "Chess.com analysis", "weekly chess report", "chess insights", "BoardSignal"],
      manifest: "/manifest.webmanifest",
      openGraph: {
        title: "BoardSignal — Your games, covered like sport",
        description: "Personal sports coverage and private performance guidance for everyday chess players.",
        siteName: "BoardSignal",
        type: "website",
      },
    };
    
    export const viewport: Viewport = {
      width: "device-width",
      initialScale: 1,
      viewportFit: "cover",
      themeColor: [
        { media: "(prefers-color-scheme: light)", color: "#f4f0e7" },
        { media: "(prefers-color-scheme: dark)", color: "#0b1220" },
      ],
    };
    
    const boardSignalThemeBootstrap = `(() => {
      const key = "boardsignal:theme";
      let choice = "system";
      try {
        const stored = window.localStorage.getItem(key);
        if (stored === "light" || stored === "dark" || stored === "system") choice = stored;
      } catch {}
      const dark = choice === "dark" || (choice === "system" && window.matchMedia("(prefers-color-scheme: dark)").matches);
      const root = document.documentElement;
      root.dataset.bsTheme = dark ? "dark" : "light";
      root.dataset.bsThemeChoice = choice;
      root.style.colorScheme = dark ? "dark" : "light";
    })();`;
    
    const trustpilotInvitationBootstrap = `(function(w,d,s,r,n){
      w.TrustpilotObject=n;
      w[n]=w[n]||function(){(w[n].q=w[n].q||[]).push(arguments)};
      var a=d.createElement(s);a.async=1;a.src=r;a.type='text/java'+s;
      var f=d.getElementsByTagName(s)[0];f.parentNode.insertBefore(a,f);
    })(window,document,'script','https://invitejs.trustpilot.com/tp.min.js','tp');
    tp('register','oiloKbZhU5G7rp2L');`;
    
    export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
      return (
        <html lang="en" className={`${montserrat.variable} ${inter.variable}`} suppressHydrationWarning>
          <head>
            <meta name="trustpilot-one-time-domain-verification-id" content="88f344e5-4f9b-4089-b887-1fb2bdbababb" />
            <script dangerouslySetInnerHTML={{ __html: boardSignalThemeBootstrap }} />
            <script dangerouslySetInnerHTML={{ __html: trustpilotInvitationBootstrap }} />
          </head>
          <body suppressHydrationWarning>
            <TrustpilotInvitationBridge />
            <AnalyticsProvider>
              <ConnectivityProvider>
                <RouteAwarePublicChrome>{children}</RouteAwarePublicChrome>
                <ServiceWorkerRegister />
                <Analytics />
                <SpeedInsights />
              </ConnectivityProvider>
            </AnalyticsProvider>
          </body>
        </html>
      );
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (/home/runner/work/adminhub-global/adminhub-global/.test-dist/tests/boardsignal-patch-h1.test.js:253:22)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: H.1 analytics configuration still expects the dedicated Vercel token
ok 93 - H.1 analytics configuration still expects the dedicated Vercel token
  ---
  duration_ms: 0.687306
  type: 'test'
  ...
# Subtest: rating boards remain pool-separated and enforce minimum games
ok 94 - rating boards remain pool-separated and enforce minimum games
  ---
  duration_ms: 1.795283
  type: 'test'
  ...
# Subtest: public recognition exposes only a positive top three
ok 95 - public recognition exposes only a positive top three
  ---
  duration_ms: 0.3349
  type: 'test'
  ...
# Subtest: all eight achievement categories exist even while an evidence field is forming
ok 96 - all eight achievement categories exist even while an evidence field is forming
  ---
  duration_ms: 0.269478
  type: 'test'
  ...
# Subtest: a current LIVE Desk replaces the matching seed only in that player's comparison
ok 97 - a current LIVE Desk replaces the matching seed only in that player's comparison
  ---
  duration_ms: 2.099795
  type: 'test'
  ...
# Subtest: fixtures never enter recognition and private Signal Board text never enters a Universe participant
ok 98 - fixtures never enter recognition and private Signal Board text never enters a Universe participant
  ---
  duration_ms: 0.230954
  type: 'test'
  ...
# Subtest: between-Desk return carries supported Blue and Amber, never Red
ok 99 - between-Desk return carries supported Blue and Amber, never Red
  ---
  duration_ms: 0.435374
  type: 'test'
  ...
1..99
# tests 99
# suites 0
# pass 93
# fail 6
# cancelled 0
# skipped 0
# todo 0
# duration_ms 938.980397
